%% Artificial Intelligence Foundations -- the full deck (one PDF).
%% Master file: preamble + title + framing, then \input the four block fragments.
%% Build:  latexmk ai_foundations.tex   (lualatex, two passes; see .latexmkrc)
%%
%% SOURCE. The whole deck is Google's "Foundational courses" track for machine
%% learning, at developers.google.com/machine-learning/foundational-courses,
%% carried over in English. The track is four courses, and the deck is four
%% blocks, one per course:
%%
%%   Block 1  Introduction to Machine Learning        (20 min course)
%%   Block 2  Machine Learning Crash Course           (12 modules)
%%   Block 3  Problem Framing                         (45 min course)
%%   Block 4  Managing ML Projects                    (90 min course)
%%
%% Every definition, worked number, table, and example keeps the course's own
%% wording; the layout is the only thing that changed. The main column holds the
%% course prose, the margin column holds the course's own asides, cross
%% references, and worked examples that the pages print inline. The courses'
%% "Key terms" footers become the grey \keyterms strip on the closing frame of
%% each module.
\documentclass[aspectratio=169]{beamer}
\usetheme{tuftedark}
\usetikzlibrary{calc,fit,shapes.geometric,positioning,arrows.meta}

% Drop slide images in images/; include by bare name, e.g.
%   \includegraphics[width=\linewidth]{pipeline}
\graphicspath{{images/}}

\title[AI Foundations]{Artificial Intelligence Foundations}
\subtitle{Google's four foundational machine learning courses, carried over}
\author[Schutera]{Prof.\ Dr.-Ing.\ Mark Schutera}
\institute{\textcolor{DHBWred}{DHBW}\ Ravensburg}
\date{\today}
\setbookversion{\today}{4 blocks}

% Right-edge vertical watermark: the sentence the whole track turns on.
\renewcommand{\tdwatermark}{Data is the driving force}

% ---------------------------------------------------------------- citations
% One global numbering for the deck, one shared References frame at the end.
% sorting=none keeps the numbers in citation order, so the sources on one slide
% compress to a tidy range, e.g. [1-3].
\usepackage[style=numeric-comp,sortcites,sorting=none,backend=biber]{biblatex}
\addbibresource{../artificialintelligencefoundations.bib}
\renewcommand*{\bibfont}{\footnotesize}
\setlength{\bibitemsep}{0.35em plus 0.15em}
\AtEveryBibitem{\clearfield{url}\clearfield{doi}\clearfield{urldate}\clearfield{note}}
\usepackage{multicol}% two-column References frame

\setbeamercolor{bibliography entry author}{fg=tddim}
\setbeamercolor{bibliography entry title}{fg=tdfg}
\setbeamercolor{bibliography entry location}{fg=tddim}
\setbeamercolor{bibliography entry note}{fg=tddim}
\setbeamercolor{bibliography item}{fg=tddim}
\DeclareFieldFormat{labelnumberwidth}{\textcolor{tddim}{#1}}

% The side-edge source marker takes cite keys and prints their numeric labels
% ([1-3]) in the same vertical spot, in place of hand-typed author-years.
\renewcommand{\slidesources}[1]{%
  \begin{tikzpicture}[remember picture,overlay]
    \node[rotate=90,anchor=west,inner sep=0pt,text=tddim,font=\scriptsize]
      at ([xshift=-3mm,yshift=13mm]current page.south east)
      {\cite{#1}};
  \end{tikzpicture}%
}

% ------------------------------------------------------------- deck helpers

% \actpage{1}{Introduction to Machine Learning}{one line on what the block is for}
% The track groups its material into four courses; beamer has no part divider,
% so a block opens with this plain frame. Numeral, rule, title, then the gloss.
\newcommand{\actpage}[3]{%
  \begin{frame}[plain]
    \vfill
    \centering
    {\color{tddim}\footnotesize BLOCK\ \ #1}\par\vskip0.4em
    {\LARGE\bfseries #2}\par\vskip0.7em
    \textcolor{DHBWred}{\rule{0.18\textwidth}{1.2pt}}\par\vskip0.7em
    {\color{tddim}\small #3}
    \vfill
  \end{frame}}

% \modulehead{Linear Regression}{80 minutes} -- frame title for the opening
% frame of an MLCC module. The module name, then its own estimated length in
% grey, exactly as the course prints it.
\newcommand{\modulehead}[2]{{#1}\;\;{\small\color{tddim}#2}}

% \keyterms{term; term; term} -- the course prints a "Key terms" footer under
% each page. Collected here into one grey strip at the foot of a module's last
% frame, so the vocabulary is visible without stealing a slide.
\newcommand{\keyterms}[1]{%
  \vskip0.6em
  {\color{tddim}\scriptsize\textsc{key terms}\quad #1\par}}

% \goal{...} -- the course's own "Learning objectives" block, set as a plain
% bordered aside rather than a bulleted wall.
\newcommand{\gterm}[1]{\textcolor{DHBWred}{#1}}

% ------------------------------------------------------------- footer nav
% The theme's footer is beamer's mini-frame navigation: one hollow dot per
% frame, grouped under its section name. It is sized for a deck of about eight
% sections; this deck runs a section per course module over some two hundred
% frames, and the dots alone would overrun the footer band. So the nav is
% replaced here, at deck level, with the same information compressed: the
% section you are in on the left, a progress track in the middle, the counter on
% the right. The theme is untouched and the other decks keep their dots.
% Drawn with \rule rather than tikz on purpose: a tikz coordinate resolves a
% bare length register through pgfmath, which hands back a unitless number that
% tikz then reads as centimetres, and the bar blows past the maximum dimension.
\newlength{\aiprogwd}
\newlength{\aiprogfill}
\setlength{\aiprogwd}{0.30\paperwidth}
% The fraction goes through pgfmath, which works in floating point. Doing it in
% TeX dimen arithmetic does not survive the first pass: before the aux file is
% written \inserttotalframenumber is stale, the \divide leaves \aiprogwd whole,
% and \aiprogwd times a frame number past about 120 exceeds the maximum TeX
% dimension. The clamp covers the same stale-aux case, where the current frame
% number can exceed the recorded total and the fill would run past its track.
\newcommand{\aiprogressbar}{%
  \ifnum\inserttotalframenumber>0\relax
    \ifnum\insertframenumber<\inserttotalframenumber\relax
      % The ratio is parenthesised so it is evaluated first. pgfmath keeps its
      % intermediates in a dimen register, so multiplying the track width by the
      % frame number before dividing overflows once past frame 120.
      \pgfmathsetlength{\aiprogfill}{\aiprogwd*(\insertframenumber/\inserttotalframenumber)}%
    \else
      \setlength{\aiprogfill}{\aiprogwd}%
    \fi
  \else
    \setlength{\aiprogfill}{0pt}%
  \fi
  \raisebox{0.6ex}{\makebox[\aiprogwd][l]{%
    \rlap{\textcolor{tdrule}{\rule{\aiprogwd}{1.1pt}}}%
    \textcolor{tdfg}{\rule{\aiprogfill}{1.1pt}}}}%
}
\setbeamertemplate{footline}{%
  \begin{beamercolorbox}[wd=\paperwidth,ht=6mm,dp=5mm,leftskip=6mm,rightskip=6mm]{section in head/foot}%
    {\usebeamerfont{section in head/foot}\color{tddim}\insertsectionhead}%
    \hfill
    \aiprogressbar
    \hfill
    {\usebeamerfont{section in head/foot}\color{tddim}%
      \insertframenumber\,/\,\inserttotalframenumber}%
  \end{beamercolorbox}%
}

\AtBeginSection[]{\begin{frame}\sectionpage\end{frame}}

\begin{document}

\begin{frame}[plain]
  \titlepage
\end{frame}

% ---------------------------------------------------------------- agenda
% The "lecture in one slide": why the track matters, and the four blocks as a
% flow rather than a table of contents.
% The agenda is one designed slide, not a table of contents: the four courses as
% a flow, each labelled with the question it answers, and one line on what you
% can do once the track is over. Full width, because four boxes plus their
% captions do not fit the 2/3 column.
\begin{frame}{Four courses, one track}
  \vfill
  \centering
  \begin{tikzpicture}[node distance=10mm and 9mm]
    \node[tdbox, font=\footnotesize, text width=22mm] (b1) {Introduction\\to ML};
    \node[tdbox, font=\footnotesize, text width=22mm, right=of b1] (b2) {Crash\\Course};
    \node[tdbox, font=\footnotesize, text width=22mm, right=of b2] (b3) {Problem\\Framing};
    \node[tdboxhi, font=\footnotesize, text width=22mm, right=of b3] (b4) {Managing\\ML Projects};
    \draw[tdarr] (b1) -- (b2);
    \draw[tdarr] (b2) -- (b3);
    \draw[tdarr] (b3) -- (b4);
    \node[below=2.5mm of b1, font=\scriptsize, text=tddim] {what ML is};
    \node[below=2.5mm of b2, font=\scriptsize, text=tddim] {how to build one};
    \node[below=2.5mm of b3, font=\scriptsize, text=tddim] {whether to at all};
    \node[below=2.5mm of b4, font=\scriptsize, text=tddim] {how to keep it alive};
  \end{tikzpicture}
  \vskip1.4em
  {\small After this track you can say whether a problem is an ML problem, frame
  it in ML terms, build and evaluate a model, and plan the project that puts it
  into production and keeps it there.}
  \vfill
  \slidesources{GoogleFoundational2025}
\end{frame}

\begin{frame}{A new way to solve problems}
  \begin{withmargin}
    \begin{tdmain}
      Machine learning powers some of the most important technologies we use,
      from translation apps to autonomous vehicles. ML offers a new way to solve
      problems, answer complex questions, and create new content. ML can predict
      the weather, estimate travel times, recommend songs, auto-complete
      sentences, summarize articles, and generate never-seen-before images.
      \vskip0.9em
      In basic terms, ML is the process of training a piece of software, called
      a \gterm{model}, to make useful predictions or generate content from data.
    \end{tdmain}
    \begin{tdmargin}
      {\color{tdfg}\textbf{Predicting rainfall, two ways.}}
      \par\medskip
      Traditional: build a physics-based representation of the atmosphere and
      surface, computing massive amounts of fluid dynamics equations. This is
      incredibly difficult.
      \par\smallskip
      ML: give the model enormous amounts of weather data until it learns the
      mathematical relationship between weather patterns that produce differing
      amounts of rain.
    \end{tdmargin}
  \end{withmargin}
  \slidesources{GoogleIntroML2025}
\end{frame}

\input{block1_intro.tex}
\input{block2_crash_course.tex}
\input{block3_problem_framing.tex}
\input{block4_managing_projects.tex}

% -------------------------------------------------------- shared references
\begin{frame}{References}
  \begin{multicols}{2}
    \printbibliography[heading=none]
  \end{multicols}
\end{frame}

\begin{frame}[plain]
  \vfill
  \centering
  {\LARGE\bfseries Data is your \alertbf{most valuable} resource}\par\vskip1.2em
  {\small Prof.\ Dr.-Ing.\ Mark Schutera \;\textbar\; DHBW Ravensburg}
  \vfill
\end{frame}

\end{document}
