% !TEX root = ../notes_unsupervisedlearning.tex
% Chapter 6: Variational Inference and VAEs
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\chapter{Variational Inference}\index{VAE}\index{Variational Autoencoder}\index{ELBO}
\newthought{embeddings and latent feature spaces.}

% ==============================================================================================
% BLOCK 1: INTRODUCTION
% ==============================================================================================

\section{The Why}\index{variational inference}\index{latent variable model}

\marginnote{\newthought{Autoencoders compress,} but VAEs generate and quantify uncertainty in a single framework.}

PCA finds linear projections; t-SNE and UMAP learn nonlinear embeddings for visualization.
Neither learns a parametric encoder that generalises to unseen inputs or generates new data.
Autoencoders close the first gap: a neural network compresses $x$ to a low-dimensional code $z$ and a second network reconstructs $\hat{x}$ from $z$.
But the resulting latent space may be irregular, with holes where no training data maps.
Sampling a random point from that space and decoding it may produce nonsense.

\newthought{Variational Autoencoders}\index{VAE}\cite{KingmaWelling2014} answer three questions at once by replacing the deterministic bottleneck with a probabilistic one.
Instead of encoding each input to a single point, the encoder outputs a distribution.
The decoder then samples from that distribution, and a regularisation term ensures the latent space is well-structured.

\newthought{In order to move from deterministic compression to probabilistic latent spaces} we have good reason to find ways to,
\begin{itemize}
    \item derive a training objective that trades off faithful reconstruction against latent-space regularity.
    \item enable gradient-based optimisation through a stochastic sampling step that would otherwise block backpropagation.
    \item understand how structural choices (KL weight, discrete versus continuous codes, conditioning variables) shape the resulting latent space and what each variant buys us.
\end{itemize}


\newpage
\subsection{Hands-On Experience}

\begin{marginfigure}
\centering
\begin{tikzpicture}
    \begin{axis}[
        xlabel={$z_1$},
        ylabel={$z_2$},
        xmin=-3, xmax=3,
        ymin=-3, ymax=3,
        width=\marginparwidth,
        height=\marginparwidth,
        axis line style={draw=none},
        tick style={black, thin},
        xtick={-2,0,2},
        ytick={-2,0,2},
        xticklabel style={font=\tiny},
        yticklabel style={font=\tiny},
        tick align=outside,
        tick pos=left,
    ]
    % Standard normal prior
    \draw[black!40, thin] (axis cs:0,0) circle[radius=1];
    \draw[black!40, thin] (axis cs:0,0) circle[radius=2];
    % Encoded distributions
    \draw[black, thick] (axis cs:-1,1) ellipse[x radius=0.3, y radius=0.5];
    \draw[black, thick] (axis cs:1,-0.5) ellipse[x radius=0.4, y radius=0.3];
    \draw[black, thick] (axis cs:0.5,1.5) ellipse[x radius=0.35, y radius=0.4];
    % Labels
    \node[font=\tiny, black!55] at (axis cs:2.3,2.3) {$p(z)$};
    \node[font=\tiny] at (axis cs:-1.8,0.5) {$q(z|x)$};
    \end{axis}
\end{tikzpicture}
\caption{VAE latent space: each input encodes to a distribution (ellipses), regularised toward the prior (circles).}
\label{fig:vae_latent_intro}
\end{marginfigure}

\newthought{Imagine} you have a 2D latent space with a standard normal prior $p(z) = \mathcal{N}(0, I)$.

Sample $z_1 = (1, 0)$ and $z_2 = (-1, 0)$.
Interpolate: $z_t = (1{-}t)\, z_1 + t\, z_2$ for $t \in [0, 1]$.
Decode each $z_t$.

What do you expect the decoded outputs to look like along this path?
Why might a standard autoencoder fail to produce a meaningful interpolation?

\newthought{The fundamental question} is: can we turn the latent bottleneck into a proper probability distribution, so that every point in the latent space decodes to something meaningful and we can generate new data by sampling?

\newthought{The Learning Objectives} of this lecture:
\begin{itemize}
    \item Derive the Evidence Lower Bound and explain how its two terms trade off reconstruction against regularisation.
    \item Implement the reparameterisation trick to enable gradient-based training through a stochastic bottleneck.
    \item Distinguish VAE variants, $\beta$-VAE, VQ-VAE, and conditional VAE, by the structural assumption each changes.
\end{itemize}


% ==============================================================================================
% BLOCK 2: MAIN THEORY
% ==============================================================================================

\newpage

\section{From Autoencoders to VAEs}\index{autoencoder}

\marginnote{\newthought{Standard autoencoders} learn $x \to z \to \hat{x}$, minimising $\|x - \hat{x}\|^2$.}

\newthought{A standard autoencoder} consists of an encoder $f_\phi\colon x \mapsto z$, a decoder $g_\theta\colon z \mapsto \hat{x}$, and a reconstruction loss $\mathcal{L} = \|x - g_\theta(f_\phi(x))\|^2$.

\newthought{Three problems emerge} once we look beyond reconstruction.
The latent space may contain holes, regions where no training point maps, and decoding a $z$ from such a region may produce nonsense.
There is no principled way to sample new data from the latent space.
And there is no measure of how uncertain a given representation is.

\begin{figure}
\centering
\begin{tikzpicture}
    \begin{axis}[
        xlabel={$z_1$},
        ylabel={$z_2$},
        xmin=-3, xmax=3,
        ymin=-3, ymax=3,
        width=0.5\textwidth,
        height=0.5\textwidth,
        axis line style={draw=none},
        tick style={black, thin},
        xtick={-2,0,2},
        ytick={-2,0,2},
        xticklabel style={font=\small},
        yticklabel style={font=\small},
        tick align=outside,
        tick pos=left,
    ]
    \addplot[only marks, mark=*, mark size=2pt, black] coordinates {
        (-2,2) (-1.8,1.5) (2,-1) (1.5,-0.5) (0,2.5) (-2,-2)
    };
    \node[font=\small, black!40] at (axis cs:0,0) {?};
    \end{axis}
\end{tikzpicture}
\caption{Autoencoder latent space. Six inputs map to six scattered points. The centre of the space contains no training encodings; decoding a $z$ sampled from that region is undefined.}
\label{fig:ae_latent}
\end{figure}

\begin{figure}
\centering
\begin{tikzpicture}
    \begin{axis}[
        xlabel={$z_1$},
        ylabel={$z_2$},
        xmin=-3, xmax=3,
        ymin=-3, ymax=3,
        width=0.5\textwidth,
        height=0.5\textwidth,
        axis line style={draw=none},
        tick style={black, thin},
        xtick={-2,0,2},
        ytick={-2,0,2},
        xticklabel style={font=\small},
        yticklabel style={font=\small},
        tick align=outside,
        tick pos=left,
    ]
    \draw[black!20, thin] (axis cs:0,0) circle[radius=1];
    \draw[black!20, thin] (axis cs:0,0) circle[radius=2];
    \addplot[only marks, mark=*, mark size=2pt, black] coordinates {
        (-1,1) (-0.5,0.8) (1,-0.5) (0.8,-0.3) (0,1.2) (-1,-1)
        (0.3,0.5) (-0.7,-0.3) (0.5,0) (-0.2,0.7)
    };
    \end{axis}
\end{tikzpicture}
\caption{VAE latent space. The KL regularisation pushes encodings toward the standard normal prior (circles at $1\sigma$ and $2\sigma$). Points fill the space; every region decodes to something meaningful.}
\label{fig:vae_latent}
\end{figure}


\newpage

\section{The Evidence Lower Bound}\index{ELBO}\index{evidence lower bound}

\marginnote{\newthought{The goal} is to maximise $\log p(x)$, but marginalising over $z$ is intractable.}

\newthought{We want to model data with latent variables.}
The marginal likelihood of an observation $x$ requires integrating over all possible latent codes:

\begin{align*}
    p_\theta(x) &= \int p_\theta(x|z)\, p(z) \, dz
\end{align*}

This integral is intractable for complex decoders.
The posterior $p_\theta(z|x)$ is equally intractable.

\newthought{Variational inference}\index{variational inference} sidesteps the intractable posterior by introducing a tractable approximation $q_\phi(z|x)$, parameterised by an encoder network.

\begin{align*}
    \log p_\theta(x) &= \log \int p_\theta(x|z)\, p(z) \, dz \\
                     &= \log \int \frac{q_\phi(z|x)}{q_\phi(z|x)}\, p_\theta(x|z)\, p(z) \, dz \\
                     &= \log \mathbb{E}_{q_\phi(z|x)} \left[ \frac{p_\theta(x|z)\, p(z)}{q_\phi(z|x)} \right]
\end{align*}

\newthought{Applying Jensen's inequality,} $\log \mathbb{E}[X] \geq \mathbb{E}[\log X]$, yields a lower bound on the log-evidence:

\begin{align*}
    \log p_\theta(x) &\geq \mathbb{E}_{q_\phi(z|x)} \left[ \log p_\theta(x|z) + \log p(z) - \log q_\phi(z|x) \right]
\end{align*}

\newthought{Rearranging gives the ELBO:}

\begin{align*}
    \text{ELBO}(\theta, \phi;\, x) &= \underbrace{\mathbb{E}_{q_\phi(z|x)}[\log p_\theta(x|z)]}_{\text{Reconstruction}} - \underbrace{D_{\text{KL}}\!\bigl(q_\phi(z|x) \,\|\, p(z)\bigr)}_{\text{Regularisation}}
\end{align*}

\marginnote{\newthought{ELBO} stands for Evidence Lower BOund. Maximising the ELBO approximately maximises $\log p(x)$.}

\newthought{The two terms trade off against each other.}
The reconstruction term rewards an encoder and decoder pair that faithfully reconstructs $x$ from a sampled $z$.
The KL term penalises encoder distributions that stray far from the prior $p(z) = \mathcal{N}(0, I)$, ensuring the latent space remains regular.

\begin{figure}
\centering
\begin{tikzpicture}
    \begin{axis}[
        xlabel={$z$},
        ylabel={density},
        xmin=-4, xmax=5,
        ymin=0, ymax=0.45,
        width=0.5\textwidth,
        height=0.4\textwidth,
        axis line style={draw=none},
        tick style={black, thin},
        xtick={-3,-1,0,1,3},
        ytick={0.1,0.2,0.3,0.4},
        xticklabel style={font=\small},
        yticklabel style={font=\small},
        tick align=outside,
        tick pos=left,
        samples=80,
        domain=-4:5,
    ]
    % Prior N(0,1)
    \addplot[black, thick] {exp(-x^2/2) / sqrt(2*pi)};
    \node[font=\small, anchor=south west] at (axis cs:1.2,0.25) {$p(z)$};
    % Shifted posterior N(2,1)
    \addplot[black!55, thick, densely dashed] {exp(-(x-2)^2/2) / sqrt(2*pi)};
    \node[font=\small, black!55, anchor=south west] at (axis cs:2.8,0.18) {$q_1$};
    % Widened posterior N(0,2)
    \addplot[black!55, thick, densely dotted] {exp(-x^2/8) / sqrt(2*pi*4)};
    \node[font=\small, black!55, anchor=south] at (axis cs:-2.5,0.12) {$q_2$};
    \end{axis}
\end{tikzpicture}
\caption{The KL penalty in action. $p(z) = \mathcal{N}(0,1)$ is the prior (solid). $q_1 = \mathcal{N}(2,1)$ (dashed) is shifted; $q_2 = \mathcal{N}(0,4)$ (dotted) is widened. Both deviations cost KL, but spreading mass away from the prior ($q_2$, $D_{\text{KL}}{=}0.81$) costs more than translating it ($q_1$, $D_{\text{KL}}{=}0.50$, evaluated in Exercise~2).}
\label{fig:kl_penalty}
\end{figure}


\newpage

\section{The Reparameterisation Trick}\index{reparameterisation trick}

\marginnote{\newthought{Sampling is not differentiable.} The reparameterisation trick moves the stochasticity outside the computational graph.}

\newthought{To optimise the ELBO} we need gradients through the expectation $\mathbb{E}_{q_\phi(z|x)}[\cdot]$.
Sampling from $q_\phi(z|x)$ blocks gradient flow because the sample operation has no derivative with respect to $\phi$.

\newthought{The fix:} instead of sampling $z \sim \mathcal{N}(\mu_\phi(x),\, \sigma_\phi^2(x))$ directly, reparameterise as

\begin{align*}
    z &= \mu_\phi(x) + \sigma_\phi(x) \odot \epsilon, \quad \epsilon \sim \mathcal{N}(0, I)
\end{align*}

Now the randomness lives in $\epsilon$, which does not depend on $\phi$.
Gradients flow through $\mu$ and $\sigma$ as usual.

\begin{figure}
\centering
\begin{tikzpicture}[
    box/.style={draw, thin, rectangle, minimum width=1.2cm, minimum height=0.6cm, font=\footnotesize},
    op/.style={draw, thin, circle, inner sep=1.5pt, font=\footnotesize},
    arr/.style={->, thin},
    node distance=1.4cm,
]
    % Main flow
    \node[box]                                              (x)     {$x$};
    \node[box, right of=x, xshift=0.2cm]                   (enc)   {Encoder};
    \node[box, right of=enc, xshift=0.3cm, yshift=0.6cm]   (mu)    {$\mu$};
    \node[box, right of=enc, xshift=0.3cm, yshift=-0.6cm]  (sigma) {$\sigma$};
    \node[op,  right of=sigma, xshift=0.2cm]                (times) {$\odot$};
    \node[op,  right of=mu, xshift=0.2cm]                   (plus)  {$+$};
    \node[box, right of=plus]                               (z)     {$z$};
    \node[box, right of=z]                                  (dec)   {Decoder};
    \node[box, right of=dec, xshift=0.2cm]                  (xhat)  {$\hat{x}$};
    % Epsilon input
    \node[font=\footnotesize] at ([yshift=-0.7cm]times.south) (eps) {$\epsilon \sim \mathcal{N}(0,I)$};
    % Arrows
    \draw[arr] (x) -- (enc);
    \draw[arr] (enc) -- (mu);
    \draw[arr] (enc) -- (sigma);
    \draw[arr] (mu) -- (plus);
    \draw[arr] (sigma) -- (times);
    \draw[arr] (eps) -- (times);
    \draw[arr] (times) -- (plus);
    \draw[arr] (plus) -- (z);
    \draw[arr] (z) -- (dec);
    \draw[arr] (dec) -- (xhat);
\end{tikzpicture}
\caption{VAE architecture with reparameterisation trick: $z = \mu + \sigma \odot \epsilon$. Stochasticity enters through $\epsilon$; all other arrows carry deterministic, differentiable operations.}
\label{fig:vae_reparam}
\end{figure}


\section{VAE Training}\index{VAE!training}

\marginnote{\newthought{The encoder} outputs $\mu_\phi(x)$ and $\log \sigma^2_\phi(x)$; the decoder outputs reconstruction parameters; the prior is $p(z) = \mathcal{N}(0, I)$.}

\newthought{For a Gaussian decoder with fixed variance,} the ELBO simplifies to a concrete loss:

\begin{align*}
    \mathcal{L}(\theta, \phi;\, x) &= \|x - \hat{x}\|^2 + D_{\text{KL}}\!\bigl(q_\phi(z|x) \,\|\, p(z)\bigr)
\end{align*}

\newthought{The KL term has a closed form} when both distributions are Gaussian:

\begin{align*}
    D_{\text{KL}}\!\bigl(\mathcal{N}(\mu, \sigma^2) \,\|\, \mathcal{N}(0, 1)\bigr) &= \tfrac{1}{2} \sum_j \bigl( \mu_j^2 + \sigma_j^2 - \log \sigma_j^2 - 1 \bigr)
\end{align*}

\marginnote{\newthought{Numerical stability.} In practice the encoder outputs $\log \sigma^2$ rather than $\sigma$ directly, avoiding negative-variance issues and improving gradient behaviour.}


\section{Exploring the Latent Space}\index{latent interpolation}\index{generation}

\newthought{Latent interpolation} reveals the structure that the VAE has learned.
Given two data points $x_1$ and $x_2$, encode them to $z_1 = \mu_\phi(x_1)$ and $z_2 = \mu_\phi(x_2)$, interpolate as $z_t = (1{-}t)\,z_1 + t\,z_2$, and decode each $z_t$ to obtain $\hat{x}_t = g_\theta(z_t)$.
Because the latent space is regularised, the decoded path transitions smoothly between the two inputs.

\begin{figure}
\centering
\begin{tikzpicture}
    \begin{axis}[
        xlabel={$z_1$},
        ylabel={$z_2$},
        xmin=-3, xmax=3,
        ymin=-3, ymax=3,
        width=0.5\textwidth,
        height=0.5\textwidth,
        axis line style={draw=none},
        tick style={black, thin},
        xtick={-2,0,2},
        ytick={-2,0,2},
        xticklabel style={font=\small},
        yticklabel style={font=\small},
        tick align=outside,
        tick pos=left,
    ]
    % Prior circles
    \draw[black!20, thin] (axis cs:0,0) circle[radius=1];
    \draw[black!20, thin] (axis cs:0,0) circle[radius=2];
    % Encodings as small ellipses
    \draw[black, thick] (axis cs:-1.5,1.2) ellipse[x radius=0.25, y radius=0.35];
    \draw[black, thick] (axis cs:1.5,-0.8) ellipse[x radius=0.3, y radius=0.25];
    % Means
    \addplot[only marks, mark=*, mark size=2pt, black] coordinates {(-1.5,1.2) (1.5,-0.8)};
    \node[font=\small, anchor=south east] at (axis cs:-1.5,1.2) {$z_1$};
    \node[font=\small, anchor=north west] at (axis cs:1.5,-0.8) {$z_2$};
    % Interpolation path
    \addplot[black!55, thick, densely dashed] coordinates {(-1.5,1.2) (1.5,-0.8)};
    % Intermediate points
    \addplot[only marks, mark=*, mark size=1.5pt, black!55] coordinates {
        (-0.9,0.72) (-0.3,0.24) (0.3,-0.24) (0.9,-0.72)
    };
    \node[font=\tiny, black!55, anchor=south] at (axis cs:0.3,-0.24) {$z_t$};
    \end{axis}
\end{tikzpicture}
\caption{Latent interpolation. Two inputs encode to $z_1$ and $z_2$ (ellipses show the encoder uncertainty). The dashed line interpolates $z_t = (1{-}t)\,z_1 + t\,z_2$; each $z_t$ is decoded to produce a smooth transition. Because the latent space is regularised, no point along the path falls into a hole.}
\label{fig:latent_interpolation}
\end{figure}

\newthought{Generation} is equally straightforward: sample $z \sim \mathcal{N}(0, I)$ and decode $\hat{x} = g_\theta(z)$.
The regularisation ensures that every region of the prior generates plausible data.


\newpage

\section{VAE Variants}\index{beta-VAE@$\beta$-VAE}\index{VQ-VAE}\index{conditional VAE}

\marginnote{\newthought{$\beta$-VAE} trades reconstruction fidelity for disentanglement by scaling the KL term.}

\begin{marginfigure}
\centering
\begin{tikzpicture}
    \begin{axis}[
        xlabel={$z_1$},
        ylabel={$z_2$},
        xmin=-3, xmax=3,
        ymin=-3, ymax=3,
        width=\marginparwidth,
        height=\marginparwidth,
        axis line style={draw=none},
        tick style={black, thin},
        xtick={-2,0,2},
        ytick={-2,0,2},
        xticklabel style={font=\tiny},
        yticklabel style={font=\tiny},
        tick align=outside,
        tick pos=left,
    ]
    % Prior circles
    \draw[black!20, thin] (axis cs:0,0) circle[radius=1];
    \draw[black!20, thin] (axis cs:0,0) circle[radius=2];
    % All posteriors collapsed to the same blob at origin
    \draw[black, thick] (axis cs:0.05,0.03) ellipse[x radius=0.15, y radius=0.18];
    \draw[black, thick] (axis cs:-0.03,0.05) ellipse[x radius=0.14, y radius=0.16];
    \draw[black, thick] (axis cs:0.02,-0.04) ellipse[x radius=0.16, y radius=0.15];
    \node[font=\tiny, black!55, anchor=west] at (axis cs:0.3,0.3) {all $q(z|x)$};
    \end{axis}
\end{tikzpicture}
\caption{Posterior collapse. With high $\beta$, all encoder distributions collapse to the prior: $q(z|x) \approx \mathcal{N}(0,I)$ for every input. The latent code carries no information about $x$.}
\label{fig:posterior_collapse}
\end{marginfigure}

\newthought{$\beta$-VAE}\cite{Higgins2017} increases the weight on the KL term:

\begin{align*}
    \mathcal{L}_\beta &= \text{Reconstruction} + \beta \cdot D_{\text{KL}}
\end{align*}

With $\beta > 1$ the model is pressured to use the latent space efficiently, often leading to disentangled\index{disentanglement} representations where each latent dimension corresponds to a single factor of variation.

\newthought{VQ-VAE}\index{VQ-VAE}\cite{vanDenOord2017} replaces continuous latents with discrete codes from a learned codebook.
Each encoder output $z_e$ is snapped to the nearest codebook entry:

\begin{align*}
    z_q &= \arg\min_{e_k \in \text{codebook}} \|z_e - e_k\|
\end{align*}

\marginnote{\newthought{VQ-VAE} enables powerful autoregressive priors, such as PixelCNN, over the discrete codes.}

Discrete codes sidestep posterior collapse entirely and enable autoregressive priors that capture complex dependencies between code positions.

\newthought{Conditional VAE}\cite{Sohn2015} adds a conditioning variable $c$, such as a class label, to both encoder $q_\phi(z|x, c)$ and decoder $p_\theta(x|z, c)$.
This enables controlled generation: sample $z$, specify $c$, and decode.


% TODO: Knowledge Distillation is only conceptually linked to VAEs ("both compress").
% Consider moving this brief to Ch8 (Self-Supervised Learning) or Ch11 (Self-Training and Ensembles),
% where teacher-student dynamics are a more natural fit.
\section{Brief: Knowledge Distillation}\index{knowledge distillation}

\marginnote{\newthought{Distillation} transfers knowledge from a large teacher to a small student.}

\newthought{Knowledge distillation}\cite{HintonDistillation2015} compresses a large teacher model into a smaller student by training the student to match the teacher's soft output distributions rather than hard labels.

\newthought{Soft labels} are produced by scaling the teacher's logits with a temperature $T$:

\begin{align*}
    p_i^{(T)} &= \frac{\exp(z_i / T)}{\sum_j \exp(z_j / T)}
\end{align*}

Higher $T$ yields a softer distribution that reveals more about the teacher's learned structure.

\newthought{The connection to VAEs} is conceptual: both methods learn compressed representations that preserve essential information, one in the latent space, the other in the student's weights.


% ==============================================================================================
% BLOCK 3: STUDENT ACTIVATION
% ==============================================================================================

\newpage

\section{Examples \& Exercises}\index{examples}\index{exercises}

\newthought{Computing by hand} builds intuition for what the ELBO actually measures.
Step through the arithmetic slowly.

\vspace{2.5em}

% ---------------------------------------------------------------------------
% Exercise 1: ELBO derivation
% ---------------------------------------------------------------------------

\newthought{ELBO derivation.} Starting from $\log p(x) = \log \int p(x,z)\, dz$, derive the ELBO.
Work through it in four steps.

\newthought{Step 1:} introduce $q(z|x)$ by multiplying and dividing inside the integral:
\begin{align*}
    \log p(x) &= \log \int \frac{q(z|x)}{q(z|x)}\, p(x,z)\, dz \\
              &= \log\, \mathbb{E}_{q(z|x)} \left[ \frac{p(x,z)}{q(z|x)} \right]
\end{align*}

\newthought{Step 2:} apply Jensen's inequality, $\log \mathbb{E}[X] \geq \mathbb{E}[\log X]$:
\begin{align*}
    \log p(x) &\geq \mathbb{E}_{q(z|x)} \left[ \log \frac{p(x,z)}{q(z|x)} \right]
\end{align*}

Complete Steps 3 and 4 yourself: split $p(x,z) = p(x|z)\,p(z)$ and rearrange to obtain the ELBO in reconstruction + KL form.
Verify that you arrive at:
\begin{align*}
    \text{ELBO} &= \underbrace{\mathbb{E}_{q(z|x)}[\log p(x|z)]}_{\text{Reconstruction}} - \underbrace{D_{\text{KL}}\!\bigl(q(z|x) \,\|\, p(z)\bigr)}_{\text{Regularisation}}
\end{align*}

\marginnote{\newthought{Why a lower bound?} Jensen's inequality introduces a gap: $\log p(x) = \text{ELBO} + D_{\text{KL}}(q \| p(z|x))$. The gap is the KL divergence between the approximate and true posterior. Maximising the ELBO simultaneously improves reconstruction and pushes $q$ toward the true posterior.}


\vspace{2.5em}

% ---------------------------------------------------------------------------
% Exercise 2: KL divergence by hand
% ---------------------------------------------------------------------------

\newthought{KL divergence by hand.}
Compute $D_{\text{KL}}\!\bigl(\mathcal{N}(\mu, \sigma^2) \,\|\, \mathcal{N}(0, 1)\bigr)$ for three settings.
Use the closed form $D_{\text{KL}} = \tfrac{1}{2}(\mu^2 + \sigma^2 - \log \sigma^2 - 1)$.

\newthought{Setting (i):} $\mu{=}0,\, \sigma{=}1$ (the posterior matches the prior exactly).
\begin{align*}
    D_{\text{KL}} &= \tfrac{1}{2}(0^2 + 1^2 - \log 1^2 - 1) \\
                  &= \tfrac{1}{2}(0 + 1 - 0 - 1) \\
                  &= 0
\end{align*}

As expected: when $q(z|x)$ exactly matches $p(z)$, the KL divergence is zero.

\newthought{Setting (ii):} $\mu{=}1,\, \sigma{=}1$ (shifted mean, same spread).
\begin{align*}
    D_{\text{KL}} &= \tfrac{1}{2}(1^2 + 1^2 - \log 1^2 - 1) \\
                  &= \tfrac{1}{2}(1 + 1 - 0 - 1) \\
                  &= 0.5
\end{align*}

The encoder shifted its mean away from zero. The prior penalises this: a shifted Gaussian wastes probability mass in regions the prior considers unlikely.

\newthought{Setting (iii):} $\mu{=}0,\, \sigma{=}2$ (correct mean, too wide).
Compute this yourself, then verify:

\begin{align*}
    D_{\text{KL}} &= \tfrac{1}{2}(0^2 + 2^2 - \log 2^2 - 1) \\
                  &= \tfrac{1}{2}(0 + 4 - 1.386 - 1) \\
                  &\approx 0.81
\end{align*}

\begin{table*}[ht]
\centering
\begin{tabular}{p{0.10\textwidth}p{0.08\textwidth}p{0.08\textwidth}p{0.10\textwidth}p{0.35\textwidth}}
    \toprule
    \textbf{Setting} & \textbf{$\mu$} & \textbf{$\sigma$} & \textbf{$D_{\text{KL}}$} & \textbf{Deviation from prior} \\
    \midrule
    (i)   & 0 & 1 & 0    & None (exact match) \\
    (ii)  & 1 & 1 & 0.50 & Shifted mean \\
    (iii) & 0 & 2 & 0.81 & Too wide \\
    \bottomrule
\end{tabular}
\vspace{2em}
\caption{KL divergence for three encoder settings. A wider posterior (iii) costs more than a shifted one (ii) because spreading probability mass away from the prior is more expensive than translating it.}
\label{tab:kl_comparison}
\end{table*}


\vspace{2.5em}

% ---------------------------------------------------------------------------
% Exercise 3: Reparameterisation
% ---------------------------------------------------------------------------

\newthought{Reparameterisation.} Why can we not directly backpropagate through $z \sim \mathcal{N}(\mu, \sigma^2)$?
Write out the reparameterised form, then verify the gradients.

\newthought{The problem:} sampling $z \sim \mathcal{N}(\mu, \sigma^2)$ is a stochastic operation with no gradient.
The parameters $\mu$ and $\sigma$ determine the distribution, but the sample itself has no differentiable relationship to them.

\newthought{The fix:} reparameterise as $z = \mu + \sigma\, \epsilon$ where $\epsilon \sim \mathcal{N}(0, 1)$.
Now $z$ is a deterministic function of $\mu$, $\sigma$, and $\epsilon$.
The randomness lives in $\epsilon$, which does not depend on the parameters.

\newthought{Numerical forward pass.} Let $\mu{=}0.5$, $\sigma{=}1.2$, and suppose $\epsilon{=}-0.3$ is sampled:
\begin{align*}
    z &= \mu + \sigma \cdot \epsilon \\
      &= 0.5 + 1.2 \times (-0.3) \\
      &= 0.5 - 0.36 \\
      &= 0.14
\end{align*}

\newthought{Verify the gradients:}
\begin{align*}
    \frac{\partial z}{\partial \mu} &= 1 \\[4pt]
    \frac{\partial z}{\partial \sigma} &= \epsilon \\
                                       &= -0.3
\end{align*}

Both are well-defined scalars.
Without reparameterisation, these gradients would not exist: backpropagation would stop at the sampling step and the encoder would receive no learning signal.


\vspace{2.5em}

% ---------------------------------------------------------------------------
% Exercise 4: ELBO evaluation by the numbers
% ---------------------------------------------------------------------------

\newthought{ELBO evaluation by the numbers.}
Combine the KL computation from the previous exercises with a concrete reconstruction to evaluate the full ELBO loss.

\newthought{Setup.} Consider a 1D VAE with encoder output $\mu{=}1,\, \sigma{=}1$ and input $x{=}3$.
After sampling and decoding, the reconstruction is $\hat{x}{=}2.7$.

\newthought{Reconstruction loss} (squared error):
\begin{align*}
    \mathcal{L}_{\text{recon}} &= (x - \hat{x})^2 \\
                                &= (3 - 2.7)^2 \\
                                &= 0.09
\end{align*}

\newthought{KL term} (from Setting (ii) above):
\begin{align*}
    D_{\text{KL}} &= 0.50
\end{align*}

\newthought{Total loss:}
\begin{align*}
    \mathcal{L} &= \mathcal{L}_{\text{recon}} + D_{\text{KL}} \\
                &= 0.09 + 0.50 \\
                &= 0.59
\end{align*}

\marginnote{\newthought{The trade-off is visible.} The reconstruction error is small (the decoder did a decent job), but the KL penalty is five times larger (the encoder strayed from the prior). A well-trained VAE balances both terms.}

Now compute the loss for a second configuration: $\mu{=}0,\, \sigma{=}1$, $x{=}3$, $\hat{x}{=}2.0$.
Which term dominates?
What does this tell you about the reconstruction-regularisation balance?

\newthought{Second configuration:}
\begin{align*}
    \mathcal{L}_{\text{recon}} &= (3 - 2.0)^2 \\
                                &= 1.00 \\[4pt]
    D_{\text{KL}} &= 0 \quad \text{(Setting (i): matches prior exactly)} \\[4pt]
    \mathcal{L} &= 1.00 + 0 \\
                &= 1.00
\end{align*}

The encoder obeys the prior perfectly ($D_{\text{KL}}{=}0$), but the reconstruction suffers.
The first configuration ($\mathcal{L}{=}0.59$) achieves a better total loss by accepting some KL cost in exchange for much better reconstruction.


\vspace{2.5em}

% ---------------------------------------------------------------------------
% Exercise 5: Posterior collapse
% ---------------------------------------------------------------------------

\newthought{Posterior collapse.} Consider a VAE trained with $\beta{=}10$ in the loss $\mathcal{L} = \mathcal{L}_{\text{recon}} + \beta \cdot D_{\text{KL}}$.
After training, you observe that $D_{\text{KL}} \approx 0$ for every input, and the reconstructions are blurry averages.
Diagnose what has happened.

\newthought{The diagnosis:} with $\beta{=}10$, the KL penalty is ten times more expensive than reconstruction.
The cheapest way to minimise the loss is to set $q(z|x) = p(z) = \mathcal{N}(0, I)$ for every input.
The encoder ignores $x$ entirely: $\mu \to 0$, $\sigma \to 1$.

\marginnote{\newthought{Verify.} If $\mu{=}0$ and $\sigma{=}1$ for all inputs, then $D_{\text{KL}}{=}0$ (Setting (i) from the KL exercise). The KL term contributes nothing. The decoder receives the same distribution regardless of $x$, so it learns to output the mean of the training data.}

\newthought{The latent code carries no information.} Sampling $z \sim \mathcal{N}(0, I)$ produces the same distribution whether the input is a ``3'' or an ``8''.
The decoder cannot distinguish inputs, so it reconstructs the average digit: a blurry blob.

\newthought{Two remedies.}
\begin{itemize}
    \item \textbf{KL annealing:} start training with $\beta{=}0$ (autoencoder mode), then slowly raise $\beta$ toward 1. The encoder first learns to use the latent space, then gradually accepts the regularisation.
    \item \textbf{Free bits:} allow each latent dimension to use at least $\lambda$ nats of KL before penalising. This guarantees the encoder encodes at least some information per dimension.
\end{itemize}


\vspace{2.5em}

% ---------------------------------------------------------------------------
% Exercise 6: Coding exercise
% ---------------------------------------------------------------------------

\marginnote{\newthought{Code} will be provided as a Python notebook.
Use it as a starting point, break things, and observe what changes.}

\newthought{An MNIST digit dataset to explore.}
Train a VAE with a 2D latent space on MNIST.
Scatter-plot the latent encodings colour-coded by digit class.
Sample a grid of points from the latent space, decode them, and display the resulting digits.
Interpolate between two digits in latent space and display the decoded path.

\newthought{What to observe.}
Digit classes should form overlapping but distinguishable clusters in the 2D latent space.
The grid of decoded samples should show smooth transitions between digit types.
Increasing $\beta$ should sharpen the cluster boundaries but blur the reconstructions.


\vspace{2.5em}

\section{Self-Reflection and Recap}\index{reflection}\index{recap}

\newthought{Self-Reflection} questions to guide your thinking:
\begin{itemize}
    \item Why does the ELBO contain two terms, and what does each one enforce?
    \item What happens if you remove the KL term entirely and train with reconstruction loss alone?
    \item Why does the reparameterisation trick work, and what mathematical property of the Gaussian does it exploit?
    \item What is posterior collapse, and under what conditions does it occur?
    \item How does $\beta$-VAE encourage disentanglement, and what is sacrificed in return?
    \item Why does VQ-VAE avoid posterior collapse, and what new challenge does discrete quantisation introduce?
    \item When would you choose a conditional VAE over an unconditional one?
    \item How does the VAE latent space differ from the PCA subspace of the previous chapter, and what does the difference buy you?
\end{itemize}

\newthought{Recap} of Key Concepts:
\begin{itemize}
    \item VAEs learn probabilistic latent representations by maximising the ELBO, which trades off reconstruction fidelity against regularisation toward the prior.
    \item The reparameterisation trick enables gradient-based training by expressing the stochastic sample as a deterministic function of the parameters plus independent noise.
    \item The closed-form KL for Gaussians makes training efficient; in practice the encoder outputs $\log \sigma^2$ for numerical stability.
    \item $\beta$-VAE increases the KL weight to encourage disentanglement at the cost of reconstruction quality; VQ-VAE replaces continuous latents with discrete codes to enable autoregressive priors.
    \item Knowledge distillation is a complementary compression strategy: it transfers a teacher's soft predictions into a smaller student network.
\end{itemize}

\marginnote{\newthought{The root of VAE blindness.} A generative model can tell you what the data looks like, but not how much to trust what it says about data it has never seen.}

\newthought{A VAE trained on coffee aroma profiles} will reconstruct familiar profiles faithfully and assign them high ELBO.
A contaminated sample, one the model has never seen, will reconstruct poorly and land in a low-density region of the latent space.
The model signals that something is off, but it cannot quantify how uncertain it is, or decide what to do about it.

\newthought{Uncertainty estimation} fills this gap.
The next chapter develops tools for quantifying what a model does and does not know, distinguishing irreducible noise from reducible ignorance, and showing how uncertainty estimates enable anomaly detection, out-of-distribution flagging, and graceful degradation in deployed systems.

\marginnote{\newthought{Teaser.} A VAE assigns a reconstruction error to every input. Is a high error sufficient to declare an input anomalous, or do we need a principled measure of what the model does not know?}
