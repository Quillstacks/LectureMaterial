% !TEX root = ../notes_unsupervisedlearning.tex
% Chapter 5: Dimensionality Reduction
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\chapter{Dimensionality Reduction}\index{dimensionality reduction}\index{PCA}\index{t-SNE}\index{UMAP}
\newthought{Folding and Unfolding Space.}

% ==============================================================================================
% BLOCK 1: INTRODUCTION
% ==============================================================================================

\section{The Why}\index{curse of dimensionality}\index{intrinsic dimensionality}

\marginnote{\newthought{MNIST handwritten digits}\cite{LeCun1998}, for example, 
live in $\mathbb{R}^{784}$ ($28{\times}28$ pixels), but vary along a handful of latent factors such as shapes or geometric primitives. 
The intrinsic dimensionality\index{intrinsic dimensionality} is 
estimated at roughly $k \approx 12$ to $14$.}
Dimensionality reduction finds those factors and discards the rest.







\newthought{Real-world data} $\mathbf{X} \in \mathbb{R}^{n \times d}$ is often high-dimensional: 
images have millions of pixels, genomes have billions of base pairs, text embeddings have thousands of dimensions.
Every clustering method in Part~I takes such a feature space as given and works within it.
But while dimensionality comes at a cost and degrades our approaches, dimensionality carries information. 
The question then becomes whether a more compact representation could carry the same information.


\newthought{Dimensionality has a price}\index{curse of dimensionality}.
In low dimensions, some points are close and others are far away; that contrast is what makes clustering work.
As $d$ grows, the contrast disappears: all pairwise distances converge toward the same value.


\newthought{Computational cost} grows with $d$, too.
Distance computations scale as $O(nd)$, covariance matrices require $O(d^{2})$ storage, and 
eigendecompositions cost $O(d^{3})$.
Reducing from $d$ to $k$ before fitting a downstream model can turn an intractable pipeline into a practical one.

\newthought{Expressiveness of the feature space.} Original features are often redundant or entangled.
If two features $x^{(i)}$ and $x^{(j)}$ are highly correlated ($|\rho_{ij}| \approx 1$), 
they carry nearly the same information; a good representation replaces them with a single component 
that captures their shared variance.

\newthought{Interpretability} benefits directly.
Humans usually usually do not think and reason in high-dimensional vector spaces; we prefer low-dimensional, 
disentangled representations that align with our concepts of space.



\newthought{In order to move from high-dimensional observations to compact, signal-carrying representations} we have good reason to find ways to,
\begin{itemize}
    \item find a mapping that retains the pattern that carries the signal.
    \item construct proxy dimensions that are not limited to the original features, so that hidden patterns become apparent.
    \item understand what different notions of preserving pattern imply, and choose the right one for a given task.
\end{itemize}












\newpage
\subsection{Hands-On Experience}

\newthought{Consider} the canonical six points, but now arranged so that $x^{(1)}$ varies 
only slightly while $x^{(2)}$ carries nearly all the spread.

\begin{figure}
\centering
\begin{tikzpicture}[scale=1]
    \begin{axis}[
        xmin=0, xmax=10,
        ymin=0, ymax=10,
        width=0.5\textwidth,
        height=0.5\textwidth,
        axis line style={draw=none},
        tick style={black, thick},
        xtick={1,2,3,4,5,6,7,8,9},
        ytick={1,2,3,4,5,6,7,8,9},
        xticklabel style={font=\small},
        yticklabel style={font=\small},
        tick align=outside,
        tick pos=left,
        xlabel={$x^{(1)}$}, ylabel={$x^{(2)}$},
    ]
    \addplot[only marks, mark=*, mark size=2pt, black] coordinates {
        (4.8,1) (5.3,2) (4.5,1.5)
    };
    \addplot[only marks, mark=*, mark size=2pt, black!55] coordinates {
        (5.2,7.5) (4.7,8.5) (5.5,9)
    };
    \node[font=\small, anchor=east]       at (axis cs:4.8,1)   {$x_1$};
    \node[font=\small, anchor=west]       at (axis cs:5.3,2)   {$x_2$};
    \node[font=\small, anchor=east]       at (axis cs:4.5,1.5) {$x_3$};
    \node[font=\small, anchor=east]       at (axis cs:5.2,7.5) {$x_4$};
    \node[font=\small, anchor=east]       at (axis cs:4.7,8.5) {$x_5$};
    \node[font=\small, anchor=west]       at (axis cs:5.5,9)   {$x_6$};
    \end{axis}
\end{tikzpicture}
\caption{The canonical six points, repositioned so that $x^{(1)}$ barely varies while $x^{(2)}$ carries both the cluster separation and the within-cluster spread. Two features describe each point, but the data is essentially one-dimensional.}
\label{fig:dimred_intro_scatter}
\end{figure}

\marginnote{
\begin{itemize}
    \item If you had to keep only one of the two features, which would you choose?
    \item How much information would you lose?
    \item And could you recover the two clusters from that single feature alone?
\end{itemize}}


\newthought{The two clusters} are clearly visible, and one feature carries almost all the signal.
The first feature $x^{(1)}$ hovers around 5 for every point; dropping it loses almost nothing.
The second feature $x^{(2)}$ alone separates the two groups and captures the within-cluster variation.


\marginnote{\newthought{Hand-crafted feature engineering} is dimensionality reduction by another name. 
When a domain expert selects "age" and "income" from a database with hundreds of columns, or computes edge gradients 
from raw pixels, they are projecting from $\mathbb{R}^{d}$ to $\mathbb{R}^{k}$ using human genius. 
}
\begin{marginfigure}
    \centering
    \includegraphics[width=0.5\marginparwidth]{./figures/eight.png}
    \caption{A single MNIST digit its meaningful variation spans few dimensions.}
    \label{fig:mnist_eight}
\end{marginfigure} 

\newthought{The methods in this chapter automate that judgment of dimensionality reduction.} 
What is the most efficient mapping you can find for the MNIST dataset, using only geometric primitives such as 
circles, lines so that each class is well-separated? 



\newthought{A well-chosen mapping} $f\colon \mathbb{R}^{d} \to \mathbb{R}^{k}$ disentangles latent factors, 
yielding coordinates that are more discriminative for downstream tasks than the originals.



\newthought{The Learning Objectives} of this lecture:
\begin{itemize}
    \item Understand naive feature selection based on variance and its limitations.
    \item Understand the PCA objective and its connection to variance maximization and the singular value decomposition.
    \item Apply t-SNE and UMAP for visualization and avoid common misinterpretations of their output.
    \item Choose appropriate dimensionality reduction methods based on linearity, dataset scale, and whether the goal is preprocessing or visualization.
\end{itemize}

















% ==============================================================================================
% BLOCK 2: MAIN THEORY
% ==============================================================================================

\newpage

\section{The Curse of Dimensionality, Revisited}\index{curse of dimensionality}

\newthought{The key observation} is that high $d$ is only fatal when all $d$ dimensions carry independent information.
In practice they rarely do.
Features are correlated, redundant, or simply noisy.
The effective degrees of freedom, the intrinsic dimensionality $k$, is often far smaller than $d$.

\begin{marginfigure}
\centering
\begin{tikzpicture}
    \begin{axis}[
        xlabel={Dimension $d$},
        ylabel={$N^{-1/d}$},
        xmin=1, xmax=50,
        ymin=0, ymax=1.05,
        width=\marginparwidth,
        height=\marginparwidth,
        axis line style={draw=none},
        tick style={black, thin},
        xtick={1,10,20,30,40,50},
        ytick={0,0.5,1},
        xticklabel style={font=\tiny},
        yticklabel style={font=\tiny},
        tick align=outside,
        tick pos=left,
    ]
    % N=100 points: N^{-1/d} = 100^{-1/d}
    \addplot[black, thin]
        coordinates {
            (1, 0.01)
            (2, 0.10)
            (3, 0.22)
            (5, 0.40)
            (7, 0.51)
            (10, 0.63)
            (15, 0.74)
            (20, 0.79)
            (30, 0.86)
            (40, 0.89)
            (50, 0.91)
        };
    \addplot[black, thin, dashed, domain=1:50] {1};
    \node[font=\tiny, anchor=south west] at (axis cs:25,0.88) {$N{=}100$};
    \end{axis}
\end{tikzpicture}
\caption{Mean nearest-neighbour spacing $d_{\mathrm{NN}} \propto N^{-1/d}$ for $N{=}100$ points. By $d{=}50$ every point is nearly as far from its neighbour as from the boundary. But if the data only occupies $k \ll d$ effective dimensions, the curse applies to $k$, not $d$.}
\label{fig:nn_spacing_curse}
\end{marginfigure}

\newthought{Reducing $d$ to $k$ reverses the curse.}
The spacing, the distance concentration, the density collapse all depend on the dimension of the space the data actually lives in.
If we project from $\mathbb{R}^{d}$ down to $\mathbb{R}^{k}$, the clustering methods from Part~I regain their footing: distances become discriminative again, $\varepsilon$-neighbourhoods fill up, and centroids sit among their members.

\marginnote{\newthought{The blessing of dimensionality.} The Johnson-Lindenstrauss lemma\index{Johnson-Lindenstrauss lemma}\cite{JohnsonLindenstrauss1984} shows that $n$ points in high dimensions can be projected to $O(\log n / \epsilon^2)$ dimensions while preserving pairwise distances within $(1{\pm}\epsilon)$. Even random projections work.}

\newthought{The question is how to find the right $k$ directions.}
Picking original features by hand (as in variance-based selection) works when the axes happen to align with the signal.
When they do not, we need methods that construct new directions from the data itself.
That is what PCA, and later t-SNE and UMAP, will do.








\vspace{1.5em}
\section{A Na\"ive Baseline: Variance-Based Feature Selection}\index{feature selection}\index{variance}

\marginnote{\newthought{Feature selection} falls into three families: \emph{filter} methods score features independently of any model (e.g.\ mutual information, variance thresholding), \emph{wrapper} methods evaluate subsets by training a predictor, and \emph{embedded} methods perform selection inside the learning objective (e.g.\ $\ell_1$ regularisation). Wrapper and embedded methods require labels; filters may or may not. Without labels we are limited to unsupervised proxies such as variance.}

\newthought{When axes align with the signal} consider the most straightforward way to reduce dimensions: 
measure the variance of each original feature independently and keep only those with the highest variance.\\

Given data $X \in \mathbb{R}^{n \times d}$, compute the variance along each coordinate axis:
\begin{align*}
    \text{Var}(x^{(j)}) &= \frac{1}{n} \sum_{i=1}^n \bigl(x_i^{(j)} - \bar{x}^{(j)}\bigr)^2, \quad j = 1, \ldots, d
\end{align*}

Sort the features by decreasing variance, keep the top $k$, and discard the rest.
This is pure feature selection: no new axes are created, only existing ones are retained or dropped.

\newthought{When does this work?}
If the coordinate axes already align with the directions of meaningful variation, variance-based selection is fast, interpretable, and surprisingly effective.
A near-constant feature carries almost no information, and removing it loses little.

\begin{marginfigure}
\centering
\begin{tikzpicture}
    \begin{axis}[
        xlabel={$x^{(1)}$}, ylabel={$x^{(2)}$},
        xmin=-4, xmax=4,
        ymin=-4, ymax=4,
        width=\marginparwidth,
        height=\marginparwidth,
        axis line style={draw=none},
        tick style={black, thin},
        xtick={-3,0,3},
        ytick={-3,0,3},
        xticklabel style={font=\tiny},
        yticklabel style={font=\tiny},
        tick align=outside,
        tick pos=left,
    ]
    \addplot[only marks, mark=*, mark size=1.5pt, black] coordinates {
        (-3,0.1) (-2.5,-0.15) (-2,0.2) (-1.5,-0.1) (-1,0.05)
        (-0.5,-0.2) (0,0.15) (0.5,-0.05) (1,0.1) (1.5,-0.15)
        (2,0.2) (2.5,-0.1) (3,0.05)
    };
    \end{axis}
\end{tikzpicture}
\caption{Data spread along $x^{(1)}$ but nearly constant in $x^{(2)}$. Dropping $x^{(2)}$ loses almost nothing.}
\label{fig:naive_aligned}
\end{marginfigure}

\newthought{When does it fail?}
When the signal does not happen to align with the axis, for example when it 
runs diagonally across the coordinate axes. In that case, no single original 
feature captures it well.

\begin{figure}
\centering
\begin{tikzpicture}
    \begin{axis}[
        xlabel={$x^{(1)}$}, ylabel={$x^{(2)}$},
        xmin=-4, xmax=4,
        ymin=-4, ymax=4,
        width=0.5\textwidth,
        height=0.5\textwidth,
        axis line style={draw=none},
        tick style={black, thin},
        xtick={-3,0,3},
        ytick={-3,0,3},
        xticklabel style={font=\small},
        yticklabel style={font=\small},
        tick align=outside,
        tick pos=left,
    ]
    % Data scattered around y=x
    \addplot[only marks, mark=*, mark size=2pt, black] coordinates {
        (-2.5,-3.1) (-2.2,-1.5) (-1.3,-1.7) (-0.7,-0.2) (-0.3,0.5)
        (0.5,-0.1) (1.2,1.6) (1.6,0.8) (2.3,2.9) (3.0,2.4)
    };
    % Projection onto x-axis
    \addplot[only marks, mark=o, mark size=2pt, black, thick] coordinates {
        (-2.5,0) (-2.2,0) (-1.3,0) (-0.7,0) (-0.3,0)
        (0.5,0) (1.2,0) (1.6,0) (2.3,0) (3.0,0)
    };
    \foreach \xa/\ya in {-2.5/-3.1, -2.2/-1.5, -1.3/-1.7, -0.7/-0.2, -0.3/0.5,
                          0.5/-0.1, 1.2/1.6, 1.6/0.8, 2.3/2.9, 3.0/2.4}{
        \addplot[black, thin, dashed] coordinates {(\xa,\ya) (\xa,0)};
    }
    \end{axis}
\end{tikzpicture}
\caption{Na\"ive feature selection: dropping $x^{(2)}$ projects onto the horizontal axis. 
The dashed errors are large because the data varies along $y{=}x$, not along a coordinate axis. 
Both features have nearly equal variance, so the na\"ive ranking cannot decide which to keep. Notice, 
how in this case both features would still preserver the signal.}
\label{fig:naive_projection}
\end{figure}


\section{Principal Component Analysis}\index{PCA}\index{covariance matrix}

\marginnote{\newthought{The manifold hypothesis}\index{manifold} holds that high-dimensional data concentrates
on a $k$-dimensional manifold $\mathcal{M} \subset \mathbb{R}^{d}$ with $k \ll d$. The goal of
dimensionality reduction is to find a coordinate system for $\mathcal{M}$.}



\newthought{Here both features have nearly the same variance,} so variance-based selection cannot decide which to keep.
Dropping either axis incurs large reconstruction error.
The real pattern lies along the diagonal $y{=}x$, a direction that no original feature represents. 
When the interesting variation runs between features, we need to construct and map to new dimensions, meaning features.
That is exactly what PCA does.


\begin{figure}
\centering
\begin{tikzpicture}
    \begin{axis}[
        xlabel={$x^{(1)}$}, ylabel={$x^{(2)}$},
        xmin=-4, xmax=4,
        ymin=-4, ymax=4,
        width=0.5\textwidth,
        height=0.5\textwidth,
        axis line style={draw=none},
        tick style={black, thin},
        xtick={-3,0,3},
        ytick={-3,0,3},
        xticklabel style={font=\small},
        yticklabel style={font=\small},
        tick align=outside,
        tick pos=left,
    ]
    % Same scattered data
    \addplot[only marks, mark=*, mark size=2pt, black] coordinates {
        (-2.5,-3.1) (-2.2,-1.5) (-1.3,-1.7) (-0.7,-0.2) (-0.3,0.5)
        (0.5,-0.1) (1.2,1.6) (1.6,0.8) (2.3,2.9) (3.0,2.4)
    };
    % PC1 tick marks and label (no connecting line)
    \draw[black,thin] (axis cs:-3.1,-2.9)--(axis cs:-2.9,-3.1);
    \draw[black,thin] (axis cs:-2.1,-1.9)--(axis cs:-1.9,-2.1);
    \draw[black,thin] (axis cs:-1.1,-0.9)--(axis cs:-0.9,-1.1);
    \draw[black,thin] (axis cs:-0.1,0.1)--(axis cs:0.1,-0.1);
    \draw[black,thin] (axis cs:0.9,1.1)--(axis cs:1.1,0.9);
    \draw[black,thin] (axis cs:1.9,2.1)--(axis cs:2.1,1.9);
    \draw[black,thin] (axis cs:2.9,3.1)--(axis cs:3.1,2.9);
    \node[font=\small] at (axis cs:3.0,-2.0) {PC1};
    % Projections onto y=x line: proj = ((x+y)/2, (x+y)/2)
    \addplot[only marks, mark=o, mark size=2pt, black, thick] coordinates {
        (-2.8,-2.8) (-1.85,-1.85) (-1.5,-1.5) (-0.45,-0.45) (0.1,0.1)
        (0.2,0.2) (1.4,1.4) (1.2,1.2) (2.6,2.6) (2.7,2.7)
    };
    % Dashed projection lines from data to PC1
    \draw[black,thin,dashed] (axis cs:-2.5,-3.1)--(axis cs:-2.8,-2.8);
    \draw[black,thin,dashed] (axis cs:-2.2,-1.5)--(axis cs:-1.85,-1.85);
    \draw[black,thin,dashed] (axis cs:-1.3,-1.7)--(axis cs:-1.5,-1.5);
    \draw[black,thin,dashed] (axis cs:-0.7,-0.2)--(axis cs:-0.45,-0.45);
    \draw[black,thin,dashed] (axis cs:-0.3,0.5)--(axis cs:0.1,0.1);
    \draw[black,thin,dashed] (axis cs:0.5,-0.1)--(axis cs:0.2,0.2);
    \draw[black,thin,dashed] (axis cs:1.2,1.6)--(axis cs:1.4,1.4);
    \draw[black,thin,dashed] (axis cs:1.6,0.8)--(axis cs:1.2,1.2);
    \draw[black,thin,dashed] (axis cs:2.3,2.9)--(axis cs:2.6,2.6);
    \draw[black,thin,dashed] (axis cs:3.0,2.4)--(axis cs:2.7,2.7);
    \end{axis}
\end{tikzpicture}
\caption{PCA projection onto PC1: The direction of maximum variance runs along $y{=}x$. 
The reconstruction errors (dashed) are tiny compared to the na\"ive projection.}
\label{fig:pca_projection}
\end{figure}

\marginnote{\newthought{PCA}\cite{Pearson1901}\cite{Hotelling1933} finds orthogonal 
directions of maximum variance in the data.}






\newthought{Given centred data} $X \in \mathbb{R}^{n \times d}$, where rows are samples, PCA finds a $k$-dimensional subspace, $k < d$, that best represents the data.

\begin{algorithm}[H]
\caption{PCA via Eigendecomposition}
\begin{algorithmic}[1]
\Require Centred data $X \in \mathbb{R}^{n \times d}$ (each column has zero mean, so that $X^T X / n$ is the covariance matrix; if features have different scales, standardise first by dividing each column by its standard deviation), target dimension $k$
\Ensure Principal components $W \in \mathbb{R}^{d \times k}$, projected data $Z \in \mathbb{R}^{n \times k}$
\State Compute covariance matrix $\Sigma = X^T X / n$
\State Compute eigendecomposition $\Sigma = V \Lambda V^T$
\State $W \gets$ first $k$ columns of $V$ (sorted by decreasing eigenvalue)
\State $Z \gets X W$ \Comment{Project data onto $k$-dimensional subspace}
\end{algorithmic}
\end{algorithm}

\newthought{The variance maximization view.}
Find the direction $w_1 \in \mathbb{R}^d$ with $\|w_1\|{=}1$ that maximises the variance of projected data:

\begin{align*}
    w_1 &= \arg\max_{\|w\|=1} \text{Var}(Xw) \\
        &= \arg\max_{\|w\|=1} w^T \Sigma\, w, \\
    \Sigma &= \tfrac{1}{n} X^T X.
\end{align*}

\newthought{Lagrange multipliers.}
Maximising $w^T \Sigma w$ subject to $w^T w = 1$ is a constrained optimisation problem.
Introduce a multiplier $\lambda$\index{Lagrange multiplier} and form the Lagrangian:

\begin{align*}
    \mathcal{L}(w,\, \lambda) &= w^T \Sigma\, w \;-\; \lambda\,(w^T w - 1)
\end{align*}

Differentiating with respect to $w$ and setting the gradient to zero:

\begin{align*}
    \frac{\partial \mathcal{L}}{\partial w}
        &= 2\,\Sigma\, w \;-\; 2\,\lambda\, w \\
        &\stackrel{!}{=}\; 0
\end{align*}

which gives the eigenvalue\index{eigenvalue} equation:

\begin{align*}
    \Sigma\, w &= \lambda\, w
\end{align*}

So $w$ must be an eigenvector\index{eigenvector} of $\Sigma$.
The projected variance along any such eigenvector equals its eigenvalue:

\begin{align*}
    w^T \Sigma\, w &= w^T (\lambda\, w) \\
                   &= \lambda\, w^T w \\
                   &= \lambda
\end{align*}

\marginnote{\newthought{Orthogonality} of principal components follows directly: eigenvectors of a symmetric matrix corresponding to distinct eigenvalues are orthogonal.}

Maximising the variance therefore means choosing $w_1$ as the eigenvector with the largest eigenvalue $\lambda_1$.
The second principal component is the eigenvector with the second-largest eigenvalue, orthogonal to the first, and so on.
The first $k$ principal components together define the optimal $k$-dimensional subspace. $k \leq d$ is a free choice.

\begin{figure}
\centering
\begin{tikzpicture}
    \begin{axis}[
        xlabel={$x^{(1)}$}, ylabel={$x^{(2)}$},
        xmin=-4, xmax=4,
        ymin=-4, ymax=4,
        width=0.5\textwidth,
        height=0.5\textwidth,
        axis line style={draw=none},
        tick style={black, thin},
        xtick={-3,0,3},
        ytick={-3,0,3},
        xticklabel style={font=\small},
        yticklabel style={font=\small},
        tick align=outside,
        tick pos=left,
    ]
    % Same scattered data
    \addplot[only marks, mark=*, mark size=2pt, black] coordinates {
        (-2.5,-3.1) (-2.2,-1.5) (-1.3,-1.7) (-0.7,-0.2) (-0.3,0.5)
        (0.5,-0.1) (1.2,1.6) (1.6,0.8) (2.3,2.9) (3.0,2.4)
    };
    % PC1 eigenvector: along y=x, i.e. (1/sqrt2, 1/sqrt2)
    \draw[-{Stealth[length=4pt,width=2.5pt]}, black!50, semithick] (axis cs:0,0) -- (axis cs:2.1,2.1);
    \node[font=\small, black!50, anchor=north west] at (axis cs:1.8,1.6) {$w_1$};
    % PC2 eigenvector: perpendicular, i.e. (1/sqrt2, -1/sqrt2)
    \draw[-{Stealth[length=4pt,width=2.5pt]}, black!50, semithick] (axis cs:0,0) -- (axis cs:0.7,-0.7);
    \node[font=\small, black!50, anchor=north west] at (axis cs:0.7,-0.7) {$w_2$};
    \end{axis}
\end{tikzpicture}
\caption{The two eigenvectors of $\Sigma$. $w_1$ points along the direction of maximum variance; $w_2$ is orthogonal and captures the residual spread.}
\label{fig:pca_eigenvectors}
\end{figure}





\newthought{Computing eigenvalues.}
Eigenvalues satisfy the characteristic equation $\det(\Sigma - \lambda I) = 0$.%
\marginnote{\newthought{Geometric intuition.}
$\Sigma$ stretches space along its eigenvector directions.
Subtracting $\lambda I$ weakens every direction by $\lambda$.
When $\lambda$ exactly matches the stretch along some eigenvector,
$\Sigma - \lambda I$ collapses that direction to zero,
the matrix becomes singular, and $\det = 0$.
So the characteristic equation asks:
at which scaling factors $\lambda$ does $\Sigma$ lose a dimension?}
For a $2{\times}2$ covariance matrix
$\Sigma = \begin{pmatrix} a & b \\ b & c \end{pmatrix}$:

\begin{align*}
    \det(\Sigma - \lambda I)
        &= \det\begin{pmatrix} a - \lambda & b \\ b & c - \lambda \end{pmatrix} \\
        &= (a - \lambda)(c - \lambda) - b^2 \\
        &= \lambda^2 - (a + c)\,\lambda + (ac - b^2) \\
        &\stackrel{!}{=}\; 0
\end{align*}

Applying the quadratic formula:

\begin{align*}
    \lambda
        &= \frac{(a + c) \pm \sqrt{(a + c)^2 - 4(ac - b^2)}}{2} \\[4pt]
        &= \frac{(a + c) \pm \sqrt{(a - c)^2 + 4b^2}}{2}
\end{align*}

\newthought{Computing eigenvectors.}
For each eigenvalue $\lambda_i$, solve $(\Sigma - \lambda_i I)\, w = 0$.
From the first row of the $2{\times}2$ system:

\begin{align*}
    (a - \lambda_i)\, w^{(1)} + b\, w^{(2)} &= 0 \\[4pt]
    w^{(2)} &= -\frac{a - \lambda_i}{b}\; w^{(1)}
\end{align*}

Any non-zero solution gives the direction; normalise to $\|w_i\|{=}1$ to obtain the principal component.


\newthought{A simple diagnostic} reveals whether the data is compressible.
Inspect the eigenvalues $\lambda_1 \geq \lambda_2 \geq \ldots \geq \lambda_d$ of $\Sigma$.\index{explained variance}
If most are near zero, the data varies in only a few directions; the remaining dimensions
add volume but no information.


The scree plot\index{scree plot} (Figure~\ref{fig:scree_plot}) visualises exactly this: a steep drop tells you how many components matter.

\begin{figure}
\centering
\begin{tikzpicture}
    \begin{axis}[
        xlabel={Component},
        ylabel={Eigenvalue $\lambda_i$},
        xmin=0.5, xmax=2.5,
        ymin=0,
        width=0.5\textwidth,
        height=0.5\textwidth,
        axis line style={draw=none},
        tick style={black, thin},
        xtick={1,2},
        xticklabel style={font=\small},
        yticklabel style={font=\small},
        tick align=outside,
        tick pos=left,
    ]
    \addplot[only marks, mark=*, mark size=2pt, black] coordinates {
        (1,4.5) (2,0.5)
    };
    % Line segment with gaps around each dot
    \draw[black, thin] (axis cs:1.15,3.90) -- (axis cs:1.85,1.10);
    \end{axis}
\end{tikzpicture}
\caption{Scree plot for the two-component example ($\lambda_1{=}4.5$, $\lambda_2{=}0.5$). The sharp drop confirms that one direction captures most of the variance.}
\label{fig:scree_plot}
\end{figure}

\begin{figure}
\centering
\begin{tikzpicture}
    \begin{axis}[
        xlabel={Components},
        ylabel={Cumulative variance},
        xmin=0.5, xmax=2.5,
        ymin=0, ymax=1.1,
        width=0.5\textwidth,
        height=0.5\textwidth,
        axis line style={draw=none},
        tick style={black, thin},
        xtick={1,2},
        ytick={0,0.5,0.9,1},
        xticklabel style={font=\small},
        yticklabel style={font=\small},
        tick align=outside,
        tick pos=left,
    ]
    \addplot[only marks, mark=*, mark size=2pt, black] coordinates {
        (1,0.90) (2,1.0)
    };
    % Line segment with gaps around each dot
    \draw[black, thin] (axis cs:1.15,0.915) -- (axis cs:1.85,0.985);
    \addplot[black, thin, dashed, domain=0.5:2.5] {0.95};
    \node[font=\small, anchor=north east] at (axis cs:2.4,0.95) {0.95};
    \end{axis}
\end{tikzpicture}
\caption{Cumulative explained variance: the fraction of total variance captured by the first $k$ components 
is $\sum_{i=1}^k \lambda_i / \sum_{i=1}^d \lambda_i$. Here PC1 alone explains 90\%; 
both components are needed to exceed the rule of thumb 95\% threshold (dashed).}
\label{fig:cumvar_plot}
\end{figure}





\newpage
\section{Reconstruction Error}\index{reconstruction error}

\newthought{So far we derived PCA from variance maximisation:} find the directions 
along which the projected data spreads the most.
There is a dual view that asks the opposite question: 
if we compress from $d$ dimensions to $k$ and then decompress back to $d$, how much signal do we lose?

\newthought{Given centred data} $X \in \mathbb{R}^{n \times d}$ and an orthonormal basis $W \in \mathbb{R}^{d \times k}$ ($W^T W = I_k$), each point $x_i$ is projected to $z_i = W^T x_i \in \mathbb{R}^k$ and reconstructed as $\hat{x}_i = W z_i = W W^T x_i$.
The reconstruction error is the squared distance between each point and its reconstruction:

\begin{align*}
    \mathcal{L}(R &= \sum_{i=1}^n \|x_i - \hat{x}_i\|^2 \\
                   &= \sum_{i=1}^n \|x_i - W W^T x_i\|^2
\end{align*}

\newthought{The dashed error} segments are short because PC1 captures the dominant 
direction of variation, making $x$ and its reconstruction similar.


\begin{figure}
\centering
\begin{tikzpicture}
    \begin{axis}[
        xlabel={$x^{(1)}$}, ylabel={$x^{(2)}$},
        xmin=-4, xmax=4,
        ymin=-4, ymax=4,
        width=0.5\textwidth,
        height=0.5\textwidth,
        axis line style={draw=none},
        tick style={black, thin},
        xtick={-3,0,3},
        ytick={-3,0,3},
        xticklabel style={font=\small},
        yticklabel style={font=\small},
        tick align=outside,
        tick pos=left,
    ]
    % Original data points
    \addplot[only marks, mark=*, mark size=2pt, black] coordinates {
        (-2.5,-3.1) (-2.2,-1.5) (-1.3,-1.7) (-0.7,-0.2) (-0.3,0.5)
        (0.5,-0.1) (1.2,1.6) (1.6,0.8) (2.3,2.9) (3.0,2.4)
    };
    % Projections onto y=x line: proj = ((x+y)/2, (x+y)/2)
    \addplot[only marks, mark=o, mark size=2pt, black!55, thick] coordinates {
        (-2.8,-2.8) (-1.85,-1.85) (-1.5,-1.5) (-0.45,-0.45) (0.1,0.1)
        (0.2,0.2) (1.4,1.4) (1.2,1.2) (2.6,2.6) (2.7,2.7)
    };
    % Dashed reconstruction error lines
    \draw[black,thin,dashed] (axis cs:-2.5,-3.1)--(axis cs:-2.8,-2.8);
    \draw[black,thin,dashed] (axis cs:-2.2,-1.5)--(axis cs:-1.85,-1.85);
    \draw[black,thin,dashed] (axis cs:-1.3,-1.7)--(axis cs:-1.5,-1.5);
    \draw[black,thin,dashed] (axis cs:-0.7,-0.2)--(axis cs:-0.45,-0.45);
    \draw[black,thin,dashed] (axis cs:-0.3,0.5)--(axis cs:0.1,0.1);
    \draw[black,thin,dashed] (axis cs:0.5,-0.1)--(axis cs:0.2,0.2);
    \draw[black,thin,dashed] (axis cs:1.2,1.6)--(axis cs:1.4,1.4);
    \draw[black,thin,dashed] (axis cs:1.6,0.8)--(axis cs:1.2,1.2);
    \draw[black,thin,dashed] (axis cs:2.3,2.9)--(axis cs:2.6,2.6);
    \draw[black,thin,dashed] (axis cs:3.0,2.4)--(axis cs:2.7,2.7);
    % Labels
    \node[font=\small, black!50] at (axis cs:3.0,-2.0) {$\tilde{x}$};
    \end{axis}
\end{tikzpicture}
\caption{Reconstruction error of PCA with $k{=}1$. Each point (filled) is projected onto PC1 (the line $y{=}x$) to get its reconstruction (open circle). The dashed segments are the reconstruction errors: the perpendicular distances that PCA minimises. Compare with Figure~\ref{fig:naive_projection}, where projection onto a coordinate axis leaves much larger errors.}
\label{fig:pca_reconstruction}
\end{figure}











\marginnote{\newthought{For $d > 2$,} closed-form eigendecompositions do not exist. In practice, use the SVD\index{SVD}: $X = U \Sigma V^T$, where the columns of $V$ are the eigenvectors of $X^T X$ with $\lambda_i = \sigma_i^2 / n$. This avoids forming $X^T X$ explicitly and is numerically stable.}

\section{Independent Component Analysis}\index{ICA}

\newthought{PCA decorrelates} but does not separate sources. 
Uncorrelated ($\text{Cov}(s_i,s_j){=}0$) is weaker than independent 
($p(s_i,s_j) = p(s_i)\,p(s_j)$). Given a linear mixture $X = AS$ of independent signals $S$, ICA\index{statistical independence} recovers $S = A^{-1}X$ by maximising non-Gaussianity\index{non-Gaussianity} of the components.




\newpage
\section{t-SNE\cite{vanDerMaaten2008}}\index{t-SNE}

\marginnote[5cm]{\newthought{PCA and ICA are linear.} No rotation of the axes can untangle classes that
live on a curved manifold. When the goal is visualization rather than preprocessing,
we need methods that can bend the coordinate system to follow the data.
This is what t-SNE and UMAP provide.}

\newthought{t-SNE}. stands for t-distributed Stochastic Neighbour
Embedding\index{embedding}. It is optimised for one-, two- or three-dimensional visualization\index{visualization}.
t-SNE preserves local pattern; nearby points in high dimensions stay nearby in the embedding.
It does this in four steps.

\begin{algorithm}[H]
\begin{algorithmic}[1]
\Require High-dimensional data $X$, perplexity, target dimension $k$

\State Compute pairwise similarities in high-D (Gaussian kernel\index{Gaussian kernel}, maps distances to $(0,1]$):
\[
  p_{j|i} = \frac{\exp\!\bigl(-\|x_i - x_j\|^2 / 2\sigma_i^2\bigr)}
                  {\sum_{l \neq i} \exp\!\bigl(-\|x_i - x_l\|^2 / 2\sigma_i^2\bigr)}
\]
Symmetrize: $p_{ij} = (p_{j|i} + p_{i|j}) / 2n$

\State Initialize low-D embedding $Y$

\Repeat

    \State Compute low-D similarities with Student-$t$ kernel\index{Student-t kernel}:
    \[
      q_{ij} = \frac{(1 + \|y_i - y_j\|^2)^{-1}}
                    {\sum_{k \neq l} (1 + \|y_k - y_l\|^2)^{-1}}
    \]

    \State Gradient step: minimise
    \[
      \text{KL}(P \| Q) = \sum_{ij} p_{ij} \log \frac{p_{ij}}{q_{ij}}
    \]

\Until{convergence}
\end{algorithmic}
\caption{t-SNE. All points are updated simultaneously (batch gradient descent). 
The KL objective is non-convex, so there is no convergence guarantee; different 
random initialisations can yield different layouts.}
\end{algorithm}

\marginnote{\newthought{Perplexity} is defined as $2^{H(P_i)}$ where $H$ is the Shannon entropy of the conditional distribution over neighbours. Think of it as the effective number of neighbours.}

\newthought{Perplexity}\index{perplexity}\index{hyperparameter selection} controls the effective number of neighbours each point considers.
It sets the bandwidth $\sigma_i$ of the Gaussian kernel.
Typical values: 5 to 50.
Higher perplexity means more global pattern is considered.



\marginnote{\newthought{The KL divergence}\index{KL divergence} $\text{KL}(P \| Q) = \sum_{ij} p_{ij} \log(p_{ij} / q_{ij})$ measures how much the low-dimensional distribution $Q$ diverges from the high-dimensional distribution $P$. It is zero when $Q = P$ and asymmetric: misrepresenting a large $p_{ij}$ with a small $q_{ij}$ is penalised heavily, so t-SNE prioritises preserving local neighbourhoods.}
\newthought{The gradient} of $\text{KL}(P \| Q)$ with respect to each embedding coordinate is

\begin{align*}
    \frac{\partial\;\text{KL}}{\partial\, y_i}
        &= 4 \sum_{j \neq i}
           (p_{ij} - q_{ij})\,(y_i - y_j)\,
           \bigl(1 + \|y_i - y_j\|^2\bigr)^{-1}
\end{align*}

Each summand acts as a pairwise force: where $p_{ij} > q_{ij}$ the force 
is attractive, where $p_{ij} < q_{ij}$ it is repulsive. 
The heavy tails of the Student-$t$ kernel give distant pairs in $Y$ a larger 
$q_{ij}$ than a Gaussian would, so the repulsive force saturates. 
This prevents the crowding problem\index{crowding problem} where all points collapse into the centre of the map.
Gradient descent\index{gradient descent} moves the embedding until local neighbourhoods in $Y$ match those in $X$.

\newthought{Trace} the four steps on our canonical six points,
mapping from $\mathbb{R}^2$ to $\mathbb{R}^1$ with perplexity${=}2$.

\begin{marginfigure}
\centering
\begin{tikzpicture}[scale=1]
    \begin{axis}[
        xmin=0, xmax=10,
        ymin=0, ymax=10,
        width=\textwidth,
        height=\textwidth,
        axis line style={draw=none},
        tick style={black, thick},
        xtick={1,2,3,6,8},
        ytick={1,2,3,7,8,9},
        xticklabel style={font=\tiny},
        yticklabel style={font=\tiny},
        tick align=outside,
        tick pos=left,
    ]
    \addplot[only marks, mark=*, mark size=2pt, black] coordinates {
        (1,2) (2,3) (3,1)
    };
    \addplot[only marks, mark=*, mark size=2pt, black!55] coordinates {
        (6,8) (8,7) (8,9)
    };
    \node[font=\tiny, anchor=south east] at (axis cs:1,2) {$x_1$};
    \node[font=\tiny, anchor=south east] at (axis cs:2,3) {$x_2$};
    \node[font=\tiny, anchor=north]      at (axis cs:3,1) {$x_3$};
    \node[font=\tiny, anchor=south west] at (axis cs:6,8) {$x_4$};
    \node[font=\tiny, anchor=north west] at (axis cs:8,7) {$x_5$};
    \node[font=\tiny, anchor=south west] at (axis cs:8,9) {$x_6$};
    \end{axis}
\end{tikzpicture}
\caption{The canonical six points (again)).
Cluster~A: $x_1{=}(1,2)$, $x_2{=}(2,3)$, $x_3{=}(3,1)$.
Cluster~B: $x_4{=}(6,8)$, $x_5{=}(8,7)$, $x_6{=}(8,9)$.}
\label{fig:tsne_canonical_ref}
\end{marginfigure}

\newthought{Step 1: Initialise the embedding.}
Each $y_i$ is drawn independently from $\mathcal{N}(0,\,10^{-4})$.
At $t{=}0$ the six points land in near-random order: cluster~A (black) and 
cluster~B (grey) are completely intermixed.

\begin{center}
\begin{tikzpicture}
    \begin{axis}[
        xmin=-6, xmax=6,
        ymin=-0.5, ymax=0.5,
        width=0.7\textwidth,
        height=3cm,
        axis line style={draw=none},
        tick style={black, thin},
        xtick={-6,-4,-2,0,2,4,6},
        ytick=\empty,
        xticklabel style={font=\small},
        tick align=outside,
        tick pos=left,
        clip=false,
        title={$t{=}0$},
        title style={font=\small, at={(0,0.5)}, anchor=east, xshift=-5pt},
    ]
    \addplot[only marks, mark=*, mark size=2pt, black] coordinates {
        (-0.4,0) (0.3,0) (-0.1,0)
    };
    \addplot[only marks, mark=*, mark size=2pt, black!55] coordinates {
        (0.5,0) (-0.5,0) (0.2,0)
    };
    \node[font=\small, anchor=south] at (axis cs:0,0.15) {$\{x_1, \ldots, x_6\}$};
    \end{axis}
\end{tikzpicture}
\end{center}

\vspace{0.5em}
\newthought{Step 2: Compute high-dimensional similarities and begin gradient descent.}
Here perplexity~$= 2$.
Same-cluster pairs get large similarities, cross-cluster pairs near zero; 
the resulting matrix $P$ is computed once and stays fixed as the target.
Gradient descent then pulls high-similarity neighbours together and pushes 
low-similarity points apart.
By $t{=}2$ most points drift toward their cluster partners, but $x_2$; initialised 
near $x_4$ (see $t{=}0$), needed a few steps for its atractive forces to move through 
the repulsive forces of the other cluster's points.

\begin{center}
\begin{tikzpicture}
    \begin{axis}[
        xmin=-6, xmax=6,
        ymin=-0.5, ymax=0.5,
        width=0.7\textwidth,
        height=3cm,
        axis line style={draw=none},
        tick style={black, thin},
        xtick={-6,-4,-2,0,2,4,6},
        ytick=\empty,
        xticklabel style={font=\small},
        tick align=outside,
        tick pos=left,
        clip=false,
        title={$t{=}2$},
        title style={font=\small, at={(0,0.5)}, anchor=east, xshift=-5pt},
    ]
    \addplot[only marks, mark=*, mark size=2pt, black] coordinates {
        (-1.4,0) (0.2,0) (-2.0,0)
    };
    \addplot[only marks, mark=*, mark size=2pt, black!55] coordinates {
        (1.4,0) (0.5,0) (1.2,0)
    };
    \node[font=\small, anchor=south] at (axis cs:-2.0,0.1) {$x_3$};
    \node[font=\small, anchor=north] at (axis cs:-1.4,-0.1) {$x_1$};
    \node[font=\small, anchor=south] at (axis cs:0.2,0.1) {$x_2$};
    \node[font=\small, anchor=north] at (axis cs:0.5,-0.1) {$x_5$};
    \node[font=\small, anchor=south] at (axis cs:1.2,0.1) {$x_6$};
    \node[font=\small, anchor=north] at (axis cs:1.4,-0.1) {$x_4$};
    \end{axis}
\end{tikzpicture}
\end{center}

\vspace{0.5em}
\newthought{Step 3: Convergence.}
At every iteration, $q_{ij}$ is recomputed from the current embedding distances.
The forces balance and the embedding stabilises.
Within each cluster the ordering mirrors similarity: $x_1$/$x_2$ (highest $p_{12}{=}0.098$) land closest in~A, $x_5$/$x_6$ ($p_{56}{=}0.089$) in~B.

\begin{center}
\begin{tikzpicture}
    \begin{axis}[
        xmin=-6, xmax=6,
        ymin=-0.5, ymax=0.5,
        width=0.7\textwidth,
        height=3cm,
        axis line style={draw=none},
        tick style={black, thin},
        xtick={-6,-4,-2,0,2,4,6},
        ytick=\empty,
        xticklabel style={font=\small},
        tick align=outside,
        tick pos=left,
        clip=false,
        title={$t{=}100$},
        title style={font=\small, at={(0,0.5)}, anchor=east, xshift=-5pt},
    ]
    \addplot[only marks, mark=*, mark size=2pt, black] coordinates {
        (-4.7,0) (-4.6,0) (-5.1,0)
    };
    \addplot[only marks, mark=*, mark size=2pt, black!55] coordinates {
        (5.0,0) (4.6,0) (4.7,0)
    };
    \node[font=\small, anchor=south] at (axis cs:-4.8,0.15) {$\{x_1, x_2, x_3\}$};
    \node[font=\small, anchor=south] at (axis cs:4.8,0.15) {$\{x_4, x_5, x_6\}$};
    \end{axis}
\end{tikzpicture}
\end{center}

\marginnote{\newthought{Interpreting t-SNE.} Cluster sizes and 
inter-cluster distances are meaningless. t-SNE equalises local densities 
and preserves only neighbourhood structure. Results vary with the random seed; 
always run multiple times. Use t-SNE for visualization only, 
never as preprocessing for downstream models.}







\section{UMAP}\index{UMAP}

\marginnote{\newthought{UMAP} stands for Uniform Manifold Approximation and Projection\cite{McInnes2018}. It has theoretical foundations in Riemannian geometry and algebraic topology.}

\newthought{UMAP brings three practical strengths.}

\newthought{Scale.} UMAP operates on a sparse $k$-nearest-neighbour graph instead of all $n^2$ pairs, giving $O(n \log n)$ complexity\index{computational complexity!UMAP} in practice, large enough for million-point datasets.

\newthought{Global structure.} UMAP optimises a cross-entropy\index{cross-entropy} loss that penalises both pulling true neighbours apart \emph{and} pushing true non-neighbours together, so the embedding preserves large-scale arrangement alongside local clusters.

\newthought{Reproducibility.} UMAP initialises the embedding with a spectral embedding\index{spectral embedding} of the graph Laplacian, so repeated runs on the same data converge to similar layouts rather than producing arbitrary rotations each time.


\vspace{1.5em}
\begin{algorithm}[H]
\caption{UMAP. Like t-SNE, all points are updated simultaneously and the objective is non-convex. The spectral initialisation makes results more reproducible than t-SNE's random start, but does not guarantee a global optimum. Key parameters: \texttt{n\_neighbors} (typically 15--200) controls local versus global structure, analogous to perplexity in t-SNE; \texttt{min\_dist} (typically 0.1--0.5) controls how tightly points cluster in the embedding.}
\begin{algorithmic}[1]
\Require High-dimensional data $X$, number of neighbours $k$, target dimension $d$, minimum distance $\delta$

\State Build a weighted $k$-nearest-neighbour graph.
For each $x_i$, set $\rho_i = \min_{j \in \text{NN}(i)} \|x_i - x_j\|$ and choose $\sigma_i$ so that
\[
  \sum_{j \in \text{NN}(i)} \exp\!\bigl(-(\|x_i - x_j\| - \rho_i) / \sigma_i\bigr) = \log_2 k
\]

\State Compute directed edge weights:
\[
  w_{j|i} = \exp\!\bigl(-\max(0,\, \|x_i - x_j\| - \rho_i) / \sigma_i\bigr)
\]
Symmetrise: $w_{ij} = w_{j|i} + w_{i|j} - w_{j|i} \cdot w_{i|j}$

\State Initialise low-D coordinates $Y$ via spectral embedding

\Repeat

    \State Compute low-D weights:
    \[
      v_{ij} = \bigl(1 + a\,\|y_i - y_j\|^{2b}\bigr)^{-1}
    \]

    \State Gradient step: minimise
    \[
      \mathcal{L} = \sum_{ij}\Bigl[ w_{ij}\log\frac{w_{ij}}{v_{ij}} + (1-w_{ij})\log\frac{1-w_{ij}}{1-v_{ij}} \Bigr]
    \]

\Until{convergence}
\end{algorithmic}
\end{algorithm}










% ==============================================================================================
% BLOCK 3: STUDENT ACTIVATION
% ==============================================================================================

\newpage

\section{Examples \& Exercises}\index{examples}\index{exercises}

\newthought{Computing by hand} builds the geometric intuition that plotting the output never can.
Step through the arithmetic slowly.

\vspace{1em}

% ---------------------------------------------------------------------------
% Exercise 1: PCA by hand on the canonical six points
% ---------------------------------------------------------------------------

\begin{marginfigure}
\centering
\begin{tikzpicture}[scale=1]
    \begin{axis}[
        xmin=0, xmax=10,
        ymin=0, ymax=10,
        width=\textwidth,
        height=\textwidth,
        axis line style={draw=none},
        tick style={black, thick},
        xtick={1,2,3,6,8},
        ytick={1,2,3,7,8,9},
        xticklabel style={font=\tiny},
        yticklabel style={font=\tiny},
        tick align=outside,
        tick pos=left,
        xlabel={$x^{(1)}$},
        ylabel={$x^{(2)}$},
    ]
    \addplot[only marks, mark=*, mark size=2pt, black] coordinates {
        (1,2) (2,3) (3,1)
    };
    \addplot[only marks, mark=*, mark size=2pt, black!55] coordinates {
        (6,8) (8,7) (8,9)
    };
    \node[font=\tiny, anchor=south east] at (axis cs:1,2) {$x_1$};
    \node[font=\tiny, anchor=south east] at (axis cs:2,3) {$x_2$};
    \node[font=\tiny, anchor=north]      at (axis cs:3,1) {$x_3$};
    \node[font=\tiny, anchor=south west] at (axis cs:6,8) {$x_4$};
    \node[font=\tiny, anchor=north west] at (axis cs:8,7) {$x_5$};
    \node[font=\tiny, anchor=south west] at (axis cs:8,9) {$x_6$};
    \end{axis}
\end{tikzpicture}
\caption{The canonical six points from Chapters~3 and~4.
Cluster~A: $x_1{=}(1,2)$, $x_2{=}(2,3)$, $x_3{=}(3,1)$.
Cluster~B: $x_4{=}(6,8)$, $x_5{=}(8,7)$, $x_6{=}(8,9)$.}
\label{fig:pca_exercise_scatter}
\end{marginfigure}

\newthought{Given the canonical six points} $x_1{=}(1,2)$, $x_2{=}(2,3)$, $x_3{=}(3,1)$, $x_4{=}(6,8)$, $x_5{=}(8,7)$, $x_6{=}(8,9)$, centre the data, compute the covariance matrix $\Sigma$, find its eigenvalues and eigenvectors, and determine what fraction of variance PC1 explains.

\newthought{Centering.} PCA requires zero-mean data. Compute the sample mean:
\begin{align*}
    \bar{x} &= \frac{1}{6}\sum_{i=1}^{6} x_i \\
             &= \left(\frac{28}{6},\; \frac{30}{6}\right) \\
             &= \left(\frac{14}{3},\; 5\right)
\end{align*}

Subtract $\bar{x}$ from each point. Here is the full calculation for $x_1$:
\begin{align*}
    \tilde{x}_1 &= x_1 - \bar{x} \\
                 &= \left(1 - \tfrac{14}{3},\; 2 - 5\right) \\
                 &= \left(-\tfrac{11}{3},\; -3\right)
\end{align*}

\vspace{3.5em}
Compute the remaining five yourself, then verify against the table below.

\begin{table*}[ht]
\centering
\begin{tabular}{p{0.07\textwidth}p{0.12\textwidth}p{0.16\textwidth}}
    \toprule
    \textbf{Point} & \textbf{Original} & \textbf{Centred $\tilde{x}_i$} \\
    \midrule
    $x_1$ & $(1,\, 2)$   & $(-11/3,\; -3)$ \\
    $x_2$ & $(2,\, 3)$   & $(-8/3,\; -2)$ \\
    $x_3$ & $(3,\, 1)$   & $(-5/3,\; -4)$ \\
    $x_4$ & $(6,\, 8)$   & $(4/3,\; 3)$ \\
    $x_5$ & $(8,\, 7)$   & $(10/3,\; 2)$ \\
    $x_6$ & $(8,\, 9)$   & $(10/3,\; 4)$ \\
    \bottomrule
\end{tabular}
\vspace{2em}
\caption{Centred data. The second coordinate centres cleanly ($\bar{x}^{(2)}{=}5$); the first introduces thirds ($\bar{x}^{(1)}{=}14/3$).}
\label{tab:pca_centred}
\end{table*}


\newpage
\newthought{The covariance matrix.} $\Sigma = \tilde{X}^T \tilde{X} / n$. Here is the full calculation for $\Sigma_{11}$:
\begin{align*}
    \Sigma_{11} &= \frac{1}{6}\sum_{i=1}^{6} \bigl(\tilde{x}_i^{(1)}\bigr)^2 \\
                &= \frac{1}{6}\left[\frac{121}{9} + \frac{64}{9} + \frac{25}{9} + \frac{16}{9} + \frac{100}{9} + \frac{100}{9}\right] \\
                &= \frac{1}{6} \cdot \frac{426}{9} \\
                &= \frac{71}{9} \\
                &\approx 7.89
\end{align*}

Compute $\Sigma_{12}$ and $\Sigma_{22}$ yourself. The full matrix:
\begin{align*}
    \Sigma &= \begin{pmatrix} 71/9 & 47/6 \\ 47/6 & 29/3 \end{pmatrix}
            \approx \begin{pmatrix} 7.89 & 7.83 \\ 7.83 & 9.67 \end{pmatrix}
\end{align*}

\marginnote{$\Sigma_{12} = 47/6 \approx 7.83$: both features are strongly positively correlated, as expected from the scatter plot where both coordinates increase together from cluster~A to cluster~B.}

\newthought{Eigenvalues.} Apply the formula from the theory section with $a = 71/9$, $b = 47/6$, $c = 29/3$:
\begin{align*}
    a + c &= \frac{71}{9} + \frac{29}{3} \\
           &= \frac{158}{9} \\
           &\approx 17.56 \\[6pt]
    (a - c)^2 + 4b^2 &= \left(\frac{-16}{9}\right)^{\!2} + 4\left(\frac{47}{6}\right)^{\!2} \\
                       &= \frac{256}{81} + \frac{2209}{9} \\
                       &= \frac{20137}{81} \\
                       &\approx 248.6
\end{align*}

So:
\begin{align*}
    \lambda &= \frac{17.56 \pm \sqrt{248.6}}{2} \\
            &\approx \frac{17.56 \pm 15.77}{2}
\end{align*}

$\lambda_1 \approx 16.66$ and $\lambda_2 \approx 0.89$.

\newthought{Eigenvector for $\lambda_1 \approx 16.66$.}
Solve $(a - \lambda_1)\,w^{(1)} + b\,w^{(2)} = 0$:
\begin{align*}
    (7.89 - 16.66)\,w^{(1)} + 7.83\,w^{(2)} &= 0 \\
    -8.77\,w^{(1)} + 7.83\,w^{(2)} &= 0 \\
    w^{(2)} &= \frac{8.77}{7.83}\,w^{(1)} \\
            &\approx 1.12\,w^{(1)}
\end{align*}

\begin{align*}
    \|w_1\| & = \sqrt{1^2 + 1.12^2} \\ 
           & = \sqrt{2.25} \\
           & = 1.50,\\
    w_1    & \approx \frac{1}{1.50}\begin{pmatrix}1 \\[3pt] 1.12\end{pmatrix}\\
           & \approx \begin{pmatrix}0.67 \\[3pt] 0.75\end{pmatrix}.
    \end{align*}

\newthought{Eigenvector for $\lambda_2 \approx 0.89$.}
Solve $(a - \lambda_2)\,w^{(1)} + b\,w^{(2)} = 0$:
\begin{align*}
    7.00\,w^{(1)} + 7.83\,w^{(2)} &= 0 \\
    w^{(2)} &\approx -0.89\,w^{(1)}
\end{align*}

Normalising: $w_2 \approx \begin{pmatrix} 0.75 \\ -0.67 \end{pmatrix}$.

\marginnote{Verify orthogonality: \\
$w_1^T w_2 = (0.67)(0.75) + (0.75)(-0.67) = 0$. \checkmark}

\newthought{Explained variance by PC1:}
\begin{align*}
    \frac{\lambda_1}{\lambda_1 + \lambda_2} &\approx \frac{16.66}{17.56} \\
                                              &\approx 95\%
\end{align*}

\marginnote{\newthought{Geometric interpretation.} PC1 points roughly along the direction 
from cluster~A (bottom left) to cluster~B (top right). The two clusters 
that are visible in two dimensions can be separated using a single principal component.}


\vspace{2.5em}

% ---------------------------------------------------------------------------
% Exercise 2: PCA projection and reconstruction
% ---------------------------------------------------------------------------

\newthought{Project and reconstruct.}
Using the eigenvectors from the previous exercise, project each of the six centred points onto PC1,
reconstruct them in the original space, and compute the reconstruction error.


\newpage
\newthought{Projection onto PC1.} For each centred point $\tilde{x}_i$, the coordinate on PC1 is $z_i = \tilde{x}_i^T w_1$.
Here is the full calculation for $x_1$:

\begin{align*}
    z_1 &= \tilde{x}_1^T w_1 \\
        &= (-3.67)(0.67) + (-3)(0.75) \\
        &= -2.46 - 2.25 \\
        &= -4.71
\end{align*}

Compute $z_2$ through $z_6$ yourself, then verify against the table below.

\newthought{Reconstruction.} Each projected point maps back to 2D via $\hat{x}_i = z_i \cdot w_1$.
For $x_1$:
\begin{align*}
    \hat{x}_1 &= -4.71 \cdot \begin{pmatrix} 0.67 \\ 0.75 \end{pmatrix} \\
              &= \begin{pmatrix} -3.16 \\ -3.53 \end{pmatrix}
\end{align*}

\newthought{Reconstruction error:}
\begin{align*}
    \|\tilde{x}_1 - \hat{x}_1\|^2 &= (-3.67 + 3.16)^2 + (-3.00 + 3.53)^2 \\
                                    &= (-0.51)^2 + (0.53)^2 \\
                                    &= 0.26 + 0.28 \\
                                    &= 0.54
\end{align*}

\begin{table*}[ht]
\centering
\begin{tabular}{p{0.07\textwidth}p{0.14\textwidth}p{0.10\textwidth}p{0.16\textwidth}p{0.16\textwidth}}
    \toprule
    \textbf{Point} & \textbf{Centred $\tilde{x}_i$} & \textbf{$z_i$} & \textbf{Reconstructed $\hat{x}_i$} & \textbf{$\|\tilde{x}_i - \hat{x}_i\|^2$} \\
    \midrule
    $x_1$ & $(-3.67,\, -3)$   & $-4.71$ & $(-3.16,\, -3.53)$ & $0.54$ \\
    $x_2$ & $(-2.67,\, -2)$   & $-3.29$ & $(-2.20,\, -2.47)$ & $0.44$ \\
    $x_3$ & $(-1.67,\, -4)$   & $-4.12$ & $(-2.76,\, -3.09)$ & $2.02$ \\
    $x_4$ & $(1.33,\, 3)$     & $3.14$  & $(2.10,\, 2.36)$   & $1.01$ \\
    $x_5$ & $(3.33,\, 2)$     & $3.73$  & $(2.50,\, 2.80)$   & $1.33$ \\
    $x_6$ & $(3.33,\, 4)$     & $5.23$  & $(3.50,\, 3.92)$   & $0.04$ \\
    \midrule
    & & & \textbf{Total} & $\mathbf{5.38}$ \\
    \bottomrule
\end{tabular}
\vspace{2em}
\caption{PCA projection onto PC1 and reconstruction for the canonical six points. The total error $\approx n \cdot \lambda_2 = 6 \times 0.89 = 5.34$ (the small difference is rounding in intermediate steps). Point $x_3$ has the largest error because it sits furthest from the PC1 direction; $x_6$ has almost none because it lies nearly on PC1.}
\label{tab:pca_projection_reconstruction}
\end{table*}


\vspace{2.5em}

% ---------------------------------------------------------------------------
% Exercise 3: Na\"ive feature selection vs PCA
% ---------------------------------------------------------------------------

\newthought{Na\"ive feature selection vs PCA.}
Using the same six centred points, compare the reconstruction error of na\"ive feature selection with PCA.

\newthought{Drop $x^{(2)}$ (keep $x^{(1)}$).} The reconstruction is $\hat{x}_i = (\tilde{x}_i^{(1)},\, 0)$.
For $x_1$:
\begin{align*}
    \|\tilde{x}_1 - \hat{x}_1\|^2 &= (-3.67-(-3.67))^2 + (-3-0)^2 \\
                                    &= 0 + 9 \\
                                    &= 9
\end{align*}

\newthought{Drop $x^{(1)}$ (keep $x^{(2)}$).} The reconstruction is $\hat{x}_i = (0,\, \tilde{x}_i^{(2)})$.
For $x_1$:
\begin{align*}
    \|\tilde{x}_1 - \hat{x}_1\|^2 &= (-3.67-0)^2 + (-3-(-3))^2 \\
                                    &= 13.47 + 0 \\
                                    &= 13.47
\end{align*}

Compute the remaining errors yourself, then verify against the table.

\begin{table*}[ht]
\centering
\begin{tabular}{p{0.07\textwidth}p{0.15\textwidth}p{0.15\textwidth}p{0.15\textwidth}}
    \toprule
    \textbf{Point} & \textbf{Drop $x^{(2)}$} & \textbf{Drop $x^{(1)}$} & \textbf{PCA (PC1)} \\
    \midrule
    $x_1$ & $9.00$  & $13.44$ & $0.54$ \\
    $x_2$ & $4.00$  & $7.11$  & $0.44$ \\
    $x_3$ & $16.00$ & $2.78$  & $2.02$ \\
    $x_4$ & $9.00$  & $1.78$  & $1.01$ \\
    $x_5$ & $4.00$  & $11.11$ & $1.33$ \\
    $x_6$ & $16.00$ & $11.11$ & $0.04$ \\
    \midrule
    \textbf{Total} & $\mathbf{58.00}$ & $\mathbf{47.33}$ & $\mathbf{5.38}$ \\
    \bottomrule
\end{tabular}
\vspace{2em}
\caption{Reconstruction error comparison. The na\"ive variance ranking correctly picks $x^{(2)}$ ($\text{Var}(x^{(2)}) \approx 9.67 > \text{Var}(x^{(1)}) \approx 7.89$), yet PCA still reduces the error by a factor of ${\approx}\,9$.}
\label{tab:naive_vs_pca}
\end{table*}

The na\"ive approach gets the ranking right: $x^{(2)}$ does carry more variance than $x^{(1)}$.
But neither coordinate axis aligns with the direction the data actually varies along.
PCA constructs that direction from the data and achieves roughly nine times less error.



% ---------------------------------------------------------------------------
% Exercise 6: Coding exercise
% ---------------------------------------------------------------------------

\marginnote{\newthought{Code} is provided as a Python notebook.
Use it as a starting point, break things, and observe what changes.}

\newthought{The Code excercises load} 1\,000 handwritten digits from MNIST: 784 pixels each, ten classes.
In code, you will apply three preprocessing stages:
discard uninformative pixels via a variance threshold,
centre the data by subtracting the per-feature mean (without centring, PC1 aligns with the mean intensity rather than with the dominant direction of variation),
and eigendecompose the covariance matrix to project onto the top two components.
Then compare the PCA projection with t-SNE (sweeping perplexity) and UMAP (sweeping neighbourhood size).

\newthought{What to observe.}
PCA preserves global geometry: The ten digit classes form broad, overlapping regions whose distances reflect the original space.
Because PCA is linear, it cannot untangle classes that live on a curved manifold; overlap in the 2D plot is a direct consequence.
t-SNE compresses each class into a tight ball, but distances between balls are arbitrary and change with the random seed.
UMAP produces similarly tight clusters while retaining more of the global arrangement.
Both nonlinear methods are sensitive to their main hyperparameter; the sweep makes this visible.



\section{Self-Reflection and Recap}\index{reflection}\index{recap}

\newthought{Self-Reflection} questions to guide your thinking:
\begin{itemize}
    \item What does it mean for data to have low intrinsic dimensionality?
    \item Why does PCA correspond to an eigenvector decomposition of the covariance matrix?
    \item What is the difference between an uncorrelated and an independent component?
    \item Why does t-SNE use a Student-$t$ kernel in the low-dimensional space but a Gaussian in the high-dimensional space?
    \item When would you prefer UMAP over t-SNE, and when would you use PCA instead of either?
    \item What does the perplexity parameter actually control, and what happens when it is too low or too high?
    \item Why should you never use t-SNE or UMAP embeddings as features for a downstream classifier?
    \item How does the curse of dimensionality motivate dimensionality reduction before running K-Means?
\end{itemize}

\newthought{Recap} of Key Concepts:
\begin{itemize}
    \item PCA finds the orthogonal directions of maximum variance via eigendecomposition of the covariance matrix; SVD is the numerically preferred implementation.
    \item The explained-variance ratio guides the choice of $k$; retain enough components to exceed a threshold such as 95\%.
    \item t-SNE and UMAP preserve local neighbourhood patterns for visualization; cluster sizes and between-cluster distances in their output are not interpretable.
    \item ICA finds statistically independent sources by maximising non-Gaussianity, going beyond the uncorrelated directions found by PCA.
    \item No non-linear method should be used as a preprocessing step for downstream learning; use PCA or SVD for that purpose.
\end{itemize}

\marginnote{\newthought{The fundamental limit} of PCA and ICA is
linearity; they cannot capture curved structure. t-SNE and UMAP learn
nonlinear embeddings, but are designed for visualization, not for
learning a reusable mapping.}

\newthought{PCA and ICA can only rotate and scale axes.}
When the underlying structure is nonlinear, no linear map can recover it.
t-SNE and UMAP solve this for visualization, but neither learns a
parametric encoder that can be applied to new data or reused as a
preprocessing step.

\marginnote{\newthought{Teaser.} If we replace the fixed eigenvector
projection with a learned, nonlinear encoder, can we get a compact
representation that generalises to unseen inputs?}
\newthought{Autoencoders} learn both the compression and the
reconstruction from data. The encoder is a neural network that maps
$x_i$ to a low-dimensional code $z_i$; the decoder maps $z_i$ back to
$\hat{x}_i$. Variational Autoencoders go one step further: the encoder
outputs not a single point but a mean $\mu$ and a variance $\sigma^2$,
turning the code into a distribution from which new data can be sampled.

\marginnote{\newthought{feedback}}
